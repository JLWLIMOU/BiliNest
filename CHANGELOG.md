# Changelog

本项目更新日志，**面向发布者**：发布者据此快速核对改动、更新 GitHub Release，并撰写面向使用者的描述。
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## 发布流程（发布者必读）

1. 把 `[Unreleased]` 改名为 `[x.y.z] - 日期`（按语义化版本决定升几位）；
2. 同步更新 `package.json` 的 `version`；
3. 每个条目**本身就是面向使用者的描述**，可直接整理成 GitHub Release 的「新增 / 修复 / 变更」列表；
4. 条目下方的「涉及文件 / 技术细节」供核对用，**不要粘贴进 GitHub Release**；
5. 发布前建议快速验证：`npm start`（或双击 `launcher.vbs`）→ 登录 → 播放一个收藏夹视频（弹幕/字幕/画质）→ 设置里确认版本号；
6. 若涉及配置/存储键/端口文件（见各条「技术细节」），在 Release 描述里提示旧数据会自动迁移，无需用户手动操作。

---

## [Unreleased]

### Other（其他）

- **学习统计**（记录 + 「学习记录」侧栏 + 详细数据）。
  - 记录口径是"**实际观看**"：播放器每次上报进度时比较「时钟增量」与「播放位置增量」，两者接近才计入 —— 拖进度条、暂停、切集都不算。数据存 `watchStats`（按天 + 按内容），和其它数据一样走 localStorage + 服务端备份，不额外落盘；只保留最近 400 天 / 300 个内容。
  - 入口在「继续学习」标题行右侧：`一共学了 X 分钟 · 学习记录 ›` —— 按用户要求显示**累计**而不是本周（累计更有成就感），且**一律按分钟**（「240 分钟」比「4 小时」更有成就感；小时只在面板内规规矩矩地显示）。**一点记录都没有（不足 1 分钟）时整个入口不出现**，标题行保持统计功能上线前的样子 —— 不摆一个「0 分钟」在那里。点击入口 → **右侧滑出侧栏**：累计大数字 + 连续打卡 / 日均 / 最多一天 + 打卡日历（近 12 周）；底部「查看更多」→ 详细页：7 / 14 / 30 天切换、每日学习时长柱状图、一周里的规律、学得最多的内容 Top 5。
  - 图表用 **uPlot 1.6.32**（MIT、零依赖、50KB + 2KB CSS，vendored 到 `public/vendor/`，见 README 的开源依赖表）；打卡日历是 CSS grid 排的格子，不是图表库 —— 实测 cal-heatmap 要 9 个依赖（对零依赖项目不划算）、frappe-charts 的月份写死英文且没有 locale 选项。日历按**日历的读法**排：一行是一周、**日子从左往右走**（周一在最左），上一行是上一周，顶上一行星期表头；格子仍是热力图那种小方块（22px、圆角 4px，居中对齐），不拉成长条。
  - 详细页的图只用来**看**，不用来拖：uPlot 默认的左键拖动是"框选缩放"（vendored 包里的默认值就是 `drag.setScale = true`），在这么窄的面板里拖一下就把横轴缩短成一截、甚至缩成一张空图 —— 看着就是"图坏了 / 图没了"。现在关掉拖动缩放，改成悬浮时在图表右上角给一行读数（`9/9 · 23 分钟`），图本身一动不动。顺带修了一个叠图问题：切 7 / 14 / 30 天是往同一个 host 里再 `new uPlot`，图会一张一张叠上去，现在重画前先 `destroy()` 再清空。
  - 样式按 Apple 的材质与动效原则：侧栏是半透明 + 背景模糊的浮动材质（§12），进出沿同一条路径（右进右出，§7），按下即刻反馈 / 松手弹回（§1、§4）；`prefers-reduced-motion` 下退化为淡入，`prefers-reduced-transparency` 下退化为实心（§14）。
  - 验证：① 播放 9 秒本地视频 → 记录 10.8 秒；跳进度条重播不计数（只 +1.5 秒 = 真实播放时间）；② 备份文件里同步出现 `watchStats`（今天 24.3 秒，与 localStorage 一致）；③ 侧栏实测贴边（`right = 1340 = innerWidth`、`top = 0`）；④ 7 / 14 / 30 天切换后图表重绘；简版与详细页均出图确认；⑤ 无记录与「只有 20 秒」两种状态下，标题行右侧都**没有任何元素**（`.study-entry` 数量为 0、无 tools 容器），有 83 分钟记录时才出现 `一共学了 83 分钟`；⑥ 播放页暂停 14 秒 → 记录 +0.0 秒，恢复播放 5 秒只加 5.1 秒（不含刚才暂停的那 14 秒），说明暂停时间不会被算进学习时长；⑦ 日历实测：格子 22×22，相邻两天 `dx = 29px / dy = 0`（往右走一天）、相邻两周 `dx = 0 / dy = 29px`（往下走一周），12 周整块 383×345 居中；⑧ 图表实测：连点 7 / 14 / 30 天四次后 `#studyDaily` 里仍只有 1 个 `.uplot` / 1 个 canvas；左键从右往左拖过整张图，横轴刻度仍是 `[0, 13]`、纵轴仍是 `[0, 42]`，图表截图与拖动前**逐像素一致**；悬浮时读数显示 `9/9 · 23 分钟`。
  - ⚠️ 两个实测踩到的坑（已修）：① 每次写入都 `Math.round()`，而播放事件约 250ms 一次（增量 0.25 秒）→ 每次都归零、**永远累加不起来**，改成保留一位小数；② 侧栏想贴边不能用 `position: fixed` —— `.overlay` 带 `backdrop-filter`，会成为 fixed 的包含块（实测顶部差 20px、右侧差 10px），改成"给有侧栏的遮罩去掉内边距 + 遮罩用 `100vw` 盖住滚动条槽位"。

- **新增维护者小工具 `tools/traffic.mjs`（`npm run traffic`）**：一行命令给出仓库的流量周报 —— 近 7 天浏览 / 独立访客（与上一周对比）、克隆、新增 star（谁点的、什么时候）、来源渠道、热门页面。
  - 数据取自 GitHub 的 traffic / stargazers 接口，需要仓库写权限，所以这是**维护者本机用的工具**；它不在 npm 包的 `files` 白名单里，不会随包分发。
  - 常用法：`npm run traffic` 直接看；`--out 文件.md` 写一份 markdown；`--json` 输出原始数据；`--install-task` 注册 Windows 计划任务（每周一 10:00 把报告写到 `%APPDATA%\BiliNest\traffic-report.md`），`--remove-task` 卸掉。
  - 另有 `--html [文件]` + `--open`：生成一份自包含的 HTML 报告（深浅色自适应、数字带涨跌色、底部带仓库 / Releases / 流量页链接）并打开；`tools/traffic-report.vbs` 是它的静默启动器（不弹控制台窗口），桌面快捷方式指向它。
  - 计划任务改成写 **HTML** 报告（`%APPDATA%\BiliNest\traffic-report.html`），和快捷方式看到的格式一致。
  - 为什么做成脚本：traffic 接口只有 14 天窗口、网页还得一层层点，这个脚本一次调用就能给出"本周 vs 上周 + 新增 star + 来源"，也让"周报"不再依赖任何 GUI。
  - 验证：`npm run traffic` 实测输出当前数据（2 star；来源 `github.com` / `link.zhihu.com`）；`--out` / `--html` 均正常写出；HTML 报告在浏览器里渲染确认（浅色卡片、涨跌色、链接可点）；`--install-task` 后 `schtasks /Query` 显示「2026/9/21 10:00 / Ready」；桌面快捷方式 `BiliNest 流量报告.lnk` 指向 `tools/traffic-report.vbs`，实测双击链路会在 `%APPDATA%\BiliNest\traffic-report.html` 生成最新报告并打开。

## [1.3.1] - 2026-09-16

### Added（新增）

- **设置里可以直接检查并一键更新**（以前只能自己去 Releases 页面翻）。整个流程只有一个按钮，用户不需要判断该点哪个。
  - `设置 → 关于 → 更新`：平时按钮是「检查更新」，查到新版本会变成「更新到 vX.Y.Z」；旁边一个下拉可选 **自动（打开页面时）/ 仅手动**（新增 `updateCheck` 设置项）。
  - 点按钮 → 确认弹窗 → 按你的装法自动走对应流程：
    - **源码版（git 检出）**：`git pull --ff-only` → 重启本地服务 → **自动刷新页面**（新前端一起生效），实测约 6 秒；本地有未提交改动会中止，不动工作区。
    - **安装包版**：自动下载 `BiliNest-x.y.z-Setup.exe`，运行安装包即可 —— 它会停掉旧服务、装完重启，数据不动。
  - 自动检查不打扰：发现新版本只在**设置图标右上角点一个小圆点**，不弹任何东西。
  - 实现：检查由本地服务代理 GitHub 的 release 接口（`GET /api/update/check`，结果缓存 10 分钟），前端不碰跨域、也不需要 token；重启接口 `POST /api/update/apply` 仅同源可写（门槛与状态备份一致）。
  - 涉及文件：`server.mjs`、`public/app.js`、`public/storage.js`、`public/styles.css`
  - 验证（无头 Edge + 真实 GitHub API）：① 已是最新 → 无圆点、面板显示"已是最新版本 v1.3.0（最新版发布于 2026-09-16）"；② 模拟旧版 1.2.0 → 打开页面即出现圆点、按钮变"更新到 v1.3.0"，点按钮 → 确认弹窗 → 确定后**服务真的重启**（`/api/health` 从 1.2.0 变 1.3.0）、页面 6 秒内自动刷新、刷新后圆点消失；③ 桩掉接口模拟安装包版（`canGitPull:false`）→ 弹窗文案为"开始下载安装包（2.3 MB）…安装程序会自动停掉旧服务并重启，数据不会动"；④ 切到"仅手动" → 打开页面不再自动检查（无圆点）。
  - ⚠️ 两个实测踩到的坑（都已修，留档）：重启辅助脚本用 `require('child_process')` 写，而本仓库 `type: module` → `node -e` 按 ESM 解析、脚本秒崩，症状是**响应成功但服务根本没重启**（当时日志里一句 `spawn is not defined` 又顺带指出漏 import 了 `spawn`）；前端"等服务回来"的判断太宽松 —— 旧进程还在应答就返回成功，页面会在旧服务上刷新一遍（看着更新成功、版本却没变），现在必须**先看到一次请求失败**或**版本号真的变了**才算重启完成。

- **支持 `npx bilinest` 直接跑**（给 npm 分发用，为将来发布到 npm 做准备）。
  - 新增 `bin/bilinest.mjs`：定位端口（`--port` 可指定，被占用时服务端自动顺延）→ 载入 `server.mjs` → 轮询 `/api/health` 确认起来了 → 打开默认浏览器（`--no-open` 可关掉）。
  - `package.json` 补 `bin` / `files` / `repository` / `homepage` / `bugs`，并去掉 `private`（带着它 npm 拒绝发布）。数据目录不变（`%APPDATA%\BiliNest`），与安装包版可以混用。
  - 验证：本机 `node bin/bilinest.mjs --no-open` 实测启动并打印访问地址；`npm pack --dry-run` 显示 25 个文件、440 KB。
  - ⚠️ 踩坑留档：Windows 上 `import(path.join(root, 'server.mjs'))` 会报 `ERR_UNSUPPORTED_ESM_URL_SCHEME` —— ESM 不接受 `D:\...` 这样的裸路径，必须用 `pathToFileURL()` 转成 `file://`。

### Fixed（修复）

- **选集面板的底部会被视口切掉**（正常路径看不出来，只有 CSS 兜底高度生效时才暴露）：兜底值 `calc(100vh - 顶栏 - 40px)` 比面板的实际剩余空间大 —— 面板的自然顶部在"返回列表"那一行下面（约 128px），超过画面高度时底部就被切掉，看起来像"列表超出、没闭合"。
  - 正常路径由 `sizeEpisodePanel()` 覆盖成"与画面等高"，这次把兜底值也改保守了：`calc(100dvh - var(--topbar-h) - 104px)`。
  - 验证：1600×960 下实测面板高 549px = 画面高 549px、底边对齐（677 / 677）、`overflowViewport: false`。

### Other（其他）

- README 更新：
  - 「更新方法」按一键更新重写（方式一 = 设置里一键更新，方式二 / 三保留 `git pull`、覆盖安装 / 覆盖解压作为手动兜底），并注明"这个按钮是较新版本才有的，旧版先手动升级一次"；
  - 新增**播放页截图**（`docs/img/player.png`，与主页截图一样是演示数据）；
  - 功能一览补「版本更新」与播放快捷键（空格 / ←→ / ↑↓ / F，进播放页即可用）、搜索与自定义标签页两条按现状改写；
  - 英文区补 `Built-in updates`；目录结构补 `bin/` 与 `docs/img/`；FAQ 补"怎么知道有没有新版本 / 怎么更新"；顶部加自动显示最新版本号的 Release 徽章。
- 界面文案与源码注释里的外部设计参照改为中性描述。

## [1.3.0] - 2026-09-16

### Fixed（修复）

- **播放页点过画面后快捷键就失效（全屏里尤其明显：必须再点一下进度条才能用空格 / 方向键）**：上一轮加的"点击播放页即恢复快捷键"漏掉了**画面**这一类点击。
  - 根因：`bindClick()` 在捕获阶段对画面上的 click 调了 `stopImmediatePropagation()`（本意是避免双击进出全屏时闪一次暂停）——**那次 click 根本到不了 document**：ArtPlayer 的 `document:click`（它才是决定 `isFocus` 的地方）和播放器那条"点击播放页就重新置位"的监听都不会跑。于是只要 `isFocus` 因为"点到播放页以外"变成 false，**点画面永远复活不了快捷键**，只有点控制条（进度条等）才行 —— 这正是用户描述的现象。
  - 复现方式（无头 Edge + 真播放器，不依赖 B 站接口）：用 `MediaRecorder` 现场造一个 1.9s 的 webm，走"本地视频"这条路进 ArtPlayer；再用合成 click 打在 `body` 上让 ArtPlayer 判定"点击落在播放器外"（`isFocus=false`）→ 按空格：`defaultPrevented=false`、播放状态不变（无效）；再真实点一下画面 → 按空格：**修复前仍然 `defaultPrevented=false`**（复现成功）。
  - 修法（`public/player.js`）：
    - 在 `#playerView` 上监听 **`pointerdown`（捕获阶段）** 来 `armHotkeys()`。`pointerdown` 比 `click` 早，且任何 click 层的拦截都影响不到它；目标落在 `INPUT` / `TEXTAREA` 上时不动，免得搅乱输入状态。
    - 监听 `fullscreenchange`：进出全屏都重新置位；**进全屏时若焦点还在输入框**（例如刚发过弹幕），先 `blur()` 再置位 —— 否则空格会被那个输入框吃掉（ArtPlayer 的快捷键检查里明确跳过 INPUT/TEXTAREA）。
  - 验证（修复后同一条路径）：① 基线（刚进播放页）按空格生效；② 合成 click 置假后按空格**确实无效**（`defaultPrevented=false`，证明状态是真关的，不是测试写错）；③ **点一下画面后按空格生效**（`defaultPrevented=true`、播放状态切换）✓；④ 再置假 → 双击画面进全屏（`document.fullscreenElement` 存在）→ **全屏里直接按空格生效** ✓。

- **自定义标签页 →「添加内容」→ 收藏夹：返回键跳过了一级**：以前进收藏夹后，返回键是"返回主页"，于是想再挑一个收藏夹、或切到视频库/UP主，得从「添加内容」第一步重新点一遍。
  - 原因：`enterFolderFromPicker()` 里 `pickerState = null` —— 选择器的分区 / 搜索词 / 勾选全被丢掉；而收藏夹的返回键固定调 `loadDashboard()`（文案也是写死的"返回主页"）。
  - 修法：① 进入收藏夹时**保留 `pickerState`**；② 返回键的文案按上下文变（`返回添加内容` / `返回主页`）；③ 点击时回到自定义标签页并把选择器**原样打开**（新增 `openContentPicker(tabId, reuse)`，分区 / 搜索词 / 勾选 / 各分区排序全部保留，连搜索框里的字也还原）；④ 收藏夹头部改为**加载一开始就渲染**（原来只在接口成功后渲染，失败时头部停在上一页的文案上），"正在添加到「X」标签页"这句也不再看收藏夹是否加载成功。
  - 涉及文件：`public/app.js`、`public/styles.css`
  - 验证（无头 Edge，真实路径：选择器里输入 `abc` → 进入收藏夹）：收藏夹内返回键文案 `返回添加内容`、标题=收藏夹名、说明=`正在添加到「日语」标签页`；点返回后 —— 主页可见且**「日语」标签仍选中**、选择器重新打开、分区仍是「源收藏夹」、搜索框仍是 `abc`；再点"取消"停在「日语」标签，不再弹任何东西。对照路径（从「收藏夹库」进收藏夹）返回键仍是 `返回主页`，点了直接回主页、不弹选择器。

- **首页切换标签时页面会被"挤一下"**（用户描述为"右侧突然出现滚轮导致些许卡顿"）：不是渲染卡，是**布局位移**。
  - 原因：各栏目内容长短不同（「视频库」20 张卡要滚、「收藏夹库」是空的不要滚），滚动条一出现就吃掉 10px。居中的 `main` 于是左右各挪半个滚动条宽，`position: sticky` 的标签栏也跟着挪 —— 切一次标签整页动 4–5px，看起来就是"卡了一下"。headless 浏览器用的是**覆盖式滚动条**（不占位），所以这个 bug 在无头环境里量不出来，得用可见窗口（真实占位滚动条）才复现。
  - 修法：`html { scrollbar-gutter: stable }` —— 让浏览器**始终预留**那条槽位：滚动条来了画在槽里，走了槽还在，内容永远不动。同一问题也发生在设置弹窗右栏（各面板长短不一），一并加了 `scrollbar-gutter: stable`。
  - 验证：可见窗口 Edge（占位滚动条，`window.innerWidth - clientWidth = 10`）实测 —— **改前**：「视频库」标签栏右边缘 492 / `main` 左边缘 25，「收藏夹库」496 / 30（相差 4–5px）；**改后**：四个标签一律 492 / 25，**位移 0px**。设置右栏四个面板左/右边缘也完全一致（差 0）。再连切 8 次视频库↔收藏夹库：**0 个 longtask、最长帧间隔 10ms**（稳定 60fps）——说明剩下的不是卡顿，之前那点"卡"就是这次位移。

- **设置弹窗切换左侧标签时，窗口会跟着内容长短"乱跳"**：四个面板高度差很大（登录与授权带上二维码最高、外观最短），而弹窗高度是"内容多高就多高"，于是点一次标签窗口缩一下、再点又弹开——窗口自己在动，读起来像界面在抖。
  - 修法：`.settings-body` **定高** `min(66vh, 560px)`，内容在右栏内部滚动。macOS 的系统设置就是定高窗口：窗口尺寸不动，只有右栏内容在滚。顺带给左栏加 `overflow-y: auto`，将来条目变多也不会撑高弹窗。
  - 涉及文件：`public/styles.css`
  - 验证：无头 Edge 在三种窗口下逐个点四个标签实测弹窗高度 —— 1280×600：467 / 467 / 467 / 467；900×700：533 ×4；1280×1000：631 ×4，**高度差全为 0**，且每次都完整落在窗口内。代价是短面板（如「关于」）底部会留一块空白 —— 这正是定高窗口的必然结果，系统的设置面板同样如此。

- **点星级打分时整张卡片跟着缩**：`:active` 是**沿祖先链匹配**的 —— 按一颗星，包含它的卡片也进入"按下"状态，于是"给视频打分"看起来像"整张卡被按了一下"。
  - 修法：让宿主持有控件在按时显式还原 —— `.card:has(:is(.stars, .card-add, .card-add-tab, .card-add-pick, .card-remove):active)`、`.hcard:has(.hcard-remove:active)` 保持在悬浮态（鼠标下 `scale(1.012)`）或原位（触摸下 `scale(1)`），只有被按的小控件自己缩。
  - 涉及文件：`public/styles.css`
  - 验证：无头 Edge 按住第一颗星实测 —— 卡片 `matrix(1.012,0,0,1.012,0,0)`（保持悬浮，不再是按下的 0.99）、星星自身 `matrix(0.9,...)`、评分写入成功（`stars: 1`）、没有误进播放页。

- **拖到分割线松手后，被拖的标签被"顺手选中"，指示条还会歪掉**：两个独立的根因，会同时出现。
  - 根因一：`setPointerCapture` 会把 `pointerup` 的 target 变成被拖的标签，于是浏览器**补发的那次 click 也落在它身上**，被标签页的点击逻辑读成"用户点了它"→ 当前标签页莫名其妙跳到刚拖过的那个。
    - 修法：拖拽结束后只吞掉"真的是拖"的这一次 click（**换位过，或指针带着标签走过 8px 以上**）；只抖几像素（<8px）仍然按点击处理，和通行的 ~10px 手势迟滞一致。
  - 根因二：取消 / 位置没变时，指示条是在**"让位回位"的过渡还没结束**时量的——`getBoundingClientRect` 读到的还是偏移后的位置，而此后没有任何时机再纠正它，滑块就永久停在错的地方。窗口窄、标签栏溢出时最明显（偏移量正好是一个标签宽）。
    - 修法：回位期间保持 `.tabs-sorting`（指示条先藏着），等 220ms 过渡结束再**对齐 + 显示**；另外落位补间结束（260ms）后再对齐一次，防止补间期间刚好滚动 / resize 把位置带偏。
  - 验证：窄窗口（880px，标签栏溢出）+ 宽窗口各跑 4 个场景（活动标签拖到分割线 / 非活动标签拖到分割线 / 中间标签拖到末尾 / 宽窗分割线）——指示条与活动标签的 left、width 误差**全部为 0**，无残留 class，落库顺序正确。

- **进播放页后必须先点一下，键盘快捷键才生效**：以前空格暂停、方向键调进度、↑↓ 音量都要先点一下播放区或进度条才起作用，否则完全没反应。顺带把触发范围放宽——现在**只要还在播放页，点任何地方（选集面板、标题区、进度条…）快捷键都保持可用**。
  - 原因：ArtPlayer 的快捷键有个隐藏前置条件——只在其内部状态 `art.isFocus` 为 `true` 时处理按键，而 `isFocus` 仅在「document click 的目标落在播放器内部」时被置位。刚进播放页时它必然是 `false`，所以按键全被忽略。
  - 修法：新增 `armHotkeys()`，在 `load()` / `loadLocal()` 载入视频后主动置位；另加一个 document 级 click 监听，凡点击落在 `#playerView`（播放页）内都重新置位——这样点选集面板之类的操作不会再让快捷键"掉线"。**两者都必须延到下一个事件循环**（`setTimeout(…, 0)`）——打开播放页的那次点击还在冒泡，ArtPlayer 的 `document:click` 处理器随后会把「目标不在播放器内」的点击判为 blur，同步设置会被它立刻覆盖。`stop()`（离开播放页）里会把 `isFocus` 关掉，避免回到主页后按空格误触发后台播放。
  - 涉及文件：`public/player.js`
  - 验证：无头 Edge 实测（`defaultPrevented === true` 即表示快捷键已接管）——① 按真实时序「点击 → 进播放页」后派发 `ArrowRight`：`true`（无需手动点击）；② 点击**播放页内、播放器之外**的标题区之后再派发：`true`（快捷键没掉线）；③ 点击返回主页后再派发：`false`（不会误触后台播放）。

- **切换标签时，标签栏总是"从最左边重新滑过来"**：正确行为应当是从**上一个标签**滑到新选中的那个。
  - 原因：每次切换标签都会重建整块标签栏（`els.dashboard.innerHTML = ...`），新建出来的横向滚动容器 `scrollLeft` 归零；`syncTabsScroll()` 再从 0 平滑滚到目标，于是看起来每次都从最左重新滑一遍。
  - 修法：重建前记下 `.dash-tabs-scroll` 的 `scrollLeft`，重建后用 `scrollTo({ behavior: 'instant' })` **瞬时**还原（这一步必须瞬时，否则等于又从上一次的位置平滑滑一次），之后才按需要平滑地把当前标签滚进视野；
  - 顺带：横向滚动过程中给标签栏加 `.scrolling`，临时关掉滑动指示条的过渡——滚动时目标每帧都在变，带过渡会让指示条滞后于标签（跟手性）；
  - 验证：造 8 个自定义标签 + 窄窗口（`scrollWidth 969 / clientWidth 484`）实测——选最后一个标签后 `scrollLeft=485`，紧接着点第一个标签时**立刻**读到的仍是 `485`（没有跳回 0），随后平滑滚到 0；滚动结束后指示条仍与标签对齐（误差 ≤1.5px）。

- **切标签时指示条"从最左边刷一下拉过来"**：上一版把下划线换成单条会滑动的指示条后，切换时它总是从最左端拉过来——系统标签之间切换也如此。
  - 原因：标签栏每次重建都会生成一个**新的**指示条节点，它从 `translateX(0) scaleX(0)` 起步，再补间到当前标签；看起来就是"从左边刷一下"。（注意这与上一条的**滚动**问题是两码事，滚动那条已经修过。）
  - 修法：记住上一次的位置 `lastIndTransform`；新节点先**关掉过渡**放回旧位置、强制一次样式计算让浏览器采纳这个起点，再恢复过渡补间到目标。首次渲染没有"上一次"，就直接就位、不做补间。
  - 顺带：用 `data-synced` 区分"新节点"与"同一个节点"——横向滚动中会被反复调用，那时走正常补间（由 `.scrolling` 关掉过渡实现 1:1 跟随），不会重复触发"回到旧位置"。
  - 验证：当前标签「视频库」位于 `x=108`，点最远的自定义标签后**立刻**读指示条的计算位置仍是 `x=108`（上一个标签处，而不是 0），最终精确落在目标标签上（误差 ≤1.5px）。

- **标签页拖拽排序"卡死"（第二次修，这次换掉了实现方式）**：上一版判断是"自动滚动的 rAF 循环空转"，加了溢出判断后仍未根治——因为真正的问题在原生拖放本身。
  - 实测结论：用真实浏览器（Edge + CDP/Playwright，**可信鼠标事件**；无头与可见窗口都试过）复现时，主线程并没有长任务（最长帧间隔 7–13ms，0 个 longtask），但拖拽过程中页面**收不到任何反馈**——`dragover` 计数有时甚至是 0，标签本身全程不动，只有一条 3px 的插入竖条。也就是说：原生 HTML5 拖放的"拖影"由浏览器生成，被拖元素不参与布局变化；标签栏又是 `sticky` + `backdrop-filter` 的浮动玻璃层，拖影生成/重栅格化在部分机器上还会明显掉帧。**用户看到的"卡死"，本质是"没有任何东西跟着手指动"。**
  - 修法：整段放弃 `draggable` + `dragstart/dragover/drop`，改为**指针事件 + transform**（`pointerdown/pointermove/pointerup`，`setPointerCapture`）：
    - 被拖的标签跟着指针走并"抬起来"（白底 + 投影；`transform` 不做过渡以保证严格跟手，抬起交给独立的 `scale` 属性，见下方微动效条目）；
    - 其余标签**实时让位**——只改 `transform`，由 CSS 过渡补间，所以是滑过去而不是跳过去；
    - 松手交给原有的 `moveCustomTab()`：它以"当前 DOM 位置（含拖动位移）"为 FLIP 起点，重排后补间到最终位置，落位也是一次连续运动；
    - 拖动过程中不改数据、不重排 DOM、不读布局（每帧只有两次 `style` 写入），成本恒定；
    - 触摸端保留"长按 320ms 才进入拖动"，否则横向拖动仍然是滚标签栏；`Esc` 取消并滑回原位；拖动期间滑块先隐去（它会停在旧位置，反而误导）。
  - 顺带修掉两个新实现里被实测抓出来的坑：① 拖动用的 `translateX` 会算进滚动容器的**可滚动宽度**（浏览器规则），于是"能滚多远"在拖动中被撑大，靠边缘自动滚动会一路跑到不存在的位置上——记下按下那一刻的 `maxScroll` 并夹住（实测：拖动中 `scrollLeft` 停在 247，而当时的 `scrollWidth` 已被撑到 278+）；② 插入位若按"让位后的位置"比较，被拖标签一旦比右邻宽，指针**还没动**就会被判成"往右挪一格"——改为与按下那一刻的原始中点比较，不动就不换位。
  - 验证（真实浏览器、可信鼠标事件）：① 拖动中采样——被拖标签 `translateX(312px)` 且带抬起投影，越过的 4 个标签同时 `translateX(-78px)` 让位，指示条 `opacity: 0`，`body.tab-sorting` 生效；② 松手后顺序正确落库（0 号插到 4 号之后）且无残留（`transform` / `.dragging` / `.tabs-sorting` 全清零）；③ 拖回原位（8px 位移）顺序不变；④ `Esc` 取消顺序不变；⑤ 单击仍选中、双击仍进入改名输入框；⑥ 全程最长帧间隔 13ms、0 个 longtask；⑦ 空闲 2s 内应用自身的 `requestAnimationFrame` 调用为 **0** 次（不再空转）；⑧ 靠边自动滚动能滚到最右，拖到末尾即"追加到最后"。

### Changed（变更）

- **从播放页返回列表时，回到原来看到的位置**（替代"把播放页开在新标签页"的方案）。
  - 涉及文件：`public/app.js`
  - 技术细节：`playVideo()` 记下"从哪个视图、什么滚动位置"进入播放页（`state.listReturn`）；播放页的「返回列表」在切回视图之后（`requestAnimationFrame`，等重排完成）恢复该位置；视图变了就不恢复。原先 `showView()` 每次都 `scrollTo(0)`，看完一集回列表要从第一屏重新找 —— 这才是这个需求真正的痛点。
  - **为什么不做"新标签页播放"（做过又撤掉了，留档）**：B 站视频可以开新标签，但本地视频不行（`blob:` 地址只在当前文档有效，必须特判）；更麻烦的是**身份重建** —— 新标签只能拿到 `bvid / cid / p`，而原流程依赖条目对象上的 `isSeries / seriesKey / title / id` 决定"续到哪一集、历史记录写成哪一条"，于是立刻冒出两个真实 bug：① 首页「继续学习」的卡片走的是另一条点击路径，不会跳转；② 新标签里的历史记录与列表不一致（贫信息对象写出来的记录自然不同）。要修好就得在 URL → 内存对象之间做一次完整重建，再叠一层跨标签同步（`store.reload()` + `storage` 事件）——**两套真相会持续冒边界 bug，收益却只是"列表不跳走"**，所以撤掉，改用上面这个 5 行的做法。
  - 验证（无头 Edge）：视频库滚到 `y=900` → 点一张**视口内**的卡片进播放页（`y=0`）→ 点「返回列表」→ `y=900` ✓；`?play=` 参数已无特殊行为（就是普通主页）；设置里不再有「播放」栏、不存在 `playerNewTab` 开关；点卡片不弹新标签（`popups: 0`）。
  - ⚠️ 测试踩坑：用 Playwright 的 `click()` 点第一张卡时它**会自动把元素滚进视口**，于是"进播放页前的滚动位置"永远是 0，看起来像功能没生效；要点视口内的卡片（用鼠标坐标点）才测得准。

- **收藏夹 / 展开全部页的头部间距**：返回按钮与标题之间原来**是 0**（`.page-title` 写着 `margin: 0`，标题直接贴在按钮下边），现在 12px；标题与下面那行说明之间不再用浏览器默认的 1em（16px），收敛到 6px。实测 12 / 6。
  - 涉及文件：`public/styles.css`

- **首页布局收口：密度、网格一致性、间距栅格**（按自查清单做的一轮，纯样式 + 一处渲染文案）。
  - 涉及文件：`public/styles.css`、`public/app.js`
  - 技术细节：
    - **「继续学习」改 3 列**：第三列的断点从 `1500px` 降到 `1100px`。主容器上限是 1200（内容宽 1156），1500 这个断点**永远不会触发** —— 于是 1280 窗口下一直是"2 列 × 569px"，封面比它下面的正文宽太多，一屏只看得到一行多一点。现在 1280 下是 3 列 ≈373px（封面 210、卡片 302 高），一屏能看到两整行 + 第三行开头。
    - **换栏目不再换网格**：「学习 UP主」原来是 `minmax(280px) + gap 14`（3 列 376px），和视频库的 4 列 274px 对不齐，切一次标签整屏列线全变。现在三处网格（视频库 / 收藏夹库 / UP主）统一 `minmax(225px, 1fr) + gap 20`，实测三栏列宽都是 274px。
    - 顺带把 UP 主卡内部收口以适配 274px：头像 56→48、内间距 14→12；卡面上的"1000 粉丝 · 10 视频"两个数字并排会被挤成省略号，改为只留主数字（粉丝），**完整统计进 `title`**（悬停可看）。
    - **空状态去掉虚线框**：虚线在系统界面的语言里是"投放目标"（拖放区），不是"这里没东西"；改成安静的居中说明（无边框、无底色），纵向留白 26→40px。
    - **间距走 8 的倍数**：`main` 顶 26→24、标签栏下 22→24、栏目内 gap 14→16（标题下 16 不变）。
    - **「展开全部」统一位置**：原来「继续学习」放在大标题行、视频库/收藏夹库放在网格下方；现在统一放网格下方，标题行只留搜索 / 排序这类真正的页级工具。
  - 验证（无头 Edge，1280×900）：继续学习 3 列 × 373（原 2 列 × 569）、卡片 302 高、封面 210；视频库 / 收藏夹库 / UP主 均为 4 列 × 274、gap 20（UP主 原 3 列 × 376）；空状态 `border-style: none`、padding `40px 20px`；间距实测 24 / 24 / 16 / 16；「展开全部（14）」位于网格下方（`top > grid.bottom`）。浅色出图确认。

- **播放页再收一轮：画面优先**（上一轮返工成浅色页之后，把"画面是主角"这件事做到底）。
  - 涉及文件：`public/styles.css`、`public/app.js`、`public/index.html`
  - 技术细节：
    - **标题最多两行**：`-webkit-line-clamp: 2`。之前遇到长标题会顶到三行（用户截图里正是如此），把 meta 一路推下去；完整标题写进 `title` 属性，悬停可看（换集时也同步更新）。
    - **播放页用更宽的容器**：`body:has(#playerView:not([hidden])) main { max-width: 1360px }`（首页仍是阅读宽度 1200）—— 同一台 1440 窗口下，画面从 1128px 变成 1316px。
    - **选集提前落到画面下方**：断点 960 → 1040。960 时左栏只剩 620px，16:9 已经很局促；现在不够宽就把选集整块挪到下面。
    - 画面与标题间距 22 → 16px。
    - 返回控件**保持页面级**（填充胶囊 + "返回列表"文字），不改位置。
  - 走过的弯路（留档）：先把返回控件做成"压在画面左上角的 38px 圆形玻璃按钮"（原生播放器 / 相册的形态）。两个问题：① 那是原生应用在触屏/遥控器上的做法，**网页端的导航控件属于页面**；② 深色半透明按钮落在同样深色的画面上会直接"消失"，用户第一反应是"返回键没了"。同理，选集面板的滚动条也曾单独改细（8px / 半透明 thumb）——它和全站那条 10px 的滚动条不是一套东西，看起来反而脏；现在统一回全站样式（`::-webkit-scrollbar` 10px + `--border-strong` + 2px `--bg` 描边）。
  - 验证：无头 Edge 实测 —— `main` 宽度 1360；返回按钮 110×34 位于 (62,84)、完全在画面之上（`bottom <= shell.top`）、底色 `--fill`、文案"返回列表"；标题 `-webkit-line-clamp: 2` 且带 `title`；1000px 窗口下变单栏、面板 `position: static`；CSSOM 里只剩全站那条滚动条规则（`.episode-panel` 的覆盖已删除）。出图确认。

- **「继续学习」的进度条去掉渐变**：进度条是"一个数值"，不是一张图。
  - 涉及文件：`public/styles.css`
  - 技术细节：填充原来是 `linear-gradient(90deg, 浅一档的强调色, 强调色)`，两端有色差 —— 结果同一个数值在不同长度下看起来是两种颜色（10% 的条偏白、90% 的条才够蓝），"看到哪儿了"读不准；在 4–5px 这么细的条上，渐变还只会显得脏。现在：**填充 = 实心强调色**（浅色 `#0071e3` / 深色 `#0a84ff`，全站同一个蓝，不再引入第二种蓝）；**轨道 = 中性的 `--fill`**（systemFill，和搜索框、排序控件同一档，不带色相）；高度 5px → 4px，两端全圆角。系统的进度指示只有两种形态：浅色面上的实心强调色、压在画面上的半透明白 —— 渐变只属于"压暗画面用的暗角"，不属于控件。（进度条上的光晕上一轮已经删掉了。）
  - 验证：无头 Edge 实测（2× 缩放出特写图）——轨道 `rgba(120,120,128,.12)`／高 4px，四条不同进度（3% / 45% / 60% / 92%）的填充一律 `rgb(0,113,227)`；深色下轨道 `rgba(120,120,128,.36)`、填充 `rgb(10,132,255)`。改前/改后同位置特写对比：改前 45% 的条左端明显发白、右端才到蓝，改后整条是同一色。

- **拖拽排序的"让位 / 落位"不再回弹**（用户反馈"过于弹"）：标签让位是被动的重排，不是用户扔出去的东西，带 2% 过冲会让人觉得整条标签栏在弹一下。
  - 涉及文件：`public/styles.css`、`public/app.js`
  - 技术细节：`.dash-tab.custom` 的 transform 过渡由 `--ease-spring-soft`（过冲到 1.02）改为 `--ease-out`；松手落位的 FLIP 由弹簧曲线改为 `cubic-bezier(0.23, 1, 0.32, 1)`、240ms。"拿起"那 3% 的放大保留（它不是回弹，是"被拿起来"的暗示）。
  - 验证：逐帧采样被让位标签的位移——`0 → -9.3（54ms）→ -66.2（109ms）→ -77.6（187ms）→ -78` 单调逼近目标，**没有任何一帧超过目标值**（此前会冲到 -79.6 再回来）。

- **悬浮不再"发光"：全站 hover 只做轻微前移 / 变色**（用户明确反馈"悬浮产生的阴影让我恶心"）。
  - 涉及文件：`public/styles.css`
  - 技术细节：
    - **卡片悬浮不再加深阴影**：`.card` / `.hcard` 的阴影是"这块表面有多厚"的陈述，悬浮时一动不动；反馈改为卡片自身 `scale(1.012)`（`transform` 单属性过渡，200ms 弹簧曲线）。一屏卡片各自亮起一圈阴影，视觉上就像一排灯在闪 —— 系统级界面的指针悬浮从来不这么做（Finder / 照片 / 官网产品磁贴都只是极轻微的前移或画面微缩放）。
    - **按下回到 < 1 的缩放**：`scale(0.99)`，与悬浮的 `1.012` 是同一串函数，逐项插值不跳。
    - **行与卡片不再改边框**：`.folder-row:hover` 去掉 `border-color` 变化（边框不是悬浮状态，忽深忽浅会让整列发颤）；`.up-card:hover` 去掉强调色描边（卡片本身已有轻微放大）。
    - **不用 filter 做 hover**：`.btn.primary:hover` 由 `filter: brightness(1.08)` 改为 `color-mix` 深一档的底色；`.fv-play:hover` 去掉 `opacity: .9`（透明度变化读起来像"禁用"）；`.stars .star:hover` 1.15 → 1.12。
    - **去掉发光**：进度条填充上的 `box-shadow: 0 0 8px` 光晕删除（系统进度填充不发光）。
  - 验证：无头 Edge 实测 —— 卡片与续播卡 hover 前后 `box-shadow` **逐字节相同**（`0px 1px 2px rgba(0,0,0,.05), 0px 1px 1px rgba(0,0,0,.03), inset 0px 1px 0px rgba(255,255,255,.7)`），`transform` 由 `matrix(1,0,0,1,0,0)` → `matrix(1.012,0,0,1.012,0,0)`，`transition-property` 只剩 `transform`。

- **首页改成"大标题"结构**（不动卡片）：一屏只有一个大标题，说明在标题下面，搜索 / 排序收进标题行。
  - 涉及文件：`public/app.js`、`public/styles.css`
  - 技术细节：
    - 新增 `.dash-page-head`（大标题 28px + 一句说明 + 右侧工具）。原来标题只有 21px、说明挤在右边当小字、搜索框还独占一整行，一屏上没有任何一处告诉用户"我在哪一页"；现在四个系统标签各自有标题（继续学习 / 视频库 / 收藏夹库 / 学习 UP主），自定义标签页用标签名。
    - 栏目内部的 `section-head` 全部去掉，只保留自定义标签页里真正的分组标题（视频 / 收藏夹库 / UP主）；`#dashSearch` 与 `#dashSort` 搬进标题行（ID 不变，事件仍走原有的委托），删掉失效的 `.section-head` / `.section-title` / `.tab-count` 样式。
    - **说明会跟着搜索走**：搜索时副标题变成"N / 总数"（如 `1 / 14 个视频`），输入时实时更新 —— 结果数就在标题下面。
    - 空状态补文案：继续学习/视频库为空时不再是一片空白（原来 `return ''`），改为一句引导。
    - 滚动行为与 iOS 大标题一致：标题随内容滚走，标签栏（`position: sticky` 的玻璃条）留在顶栏下方。
  - 验证：无头 Edge 实测 —— 切换四个系统标签标题/说明分别为「继续学习·自动从上次进度续播」「视频库·共 14 个视频」「收藏夹库·把收藏夹加进来单独管理」「学习 UP主·输入 UID 添加要跟的 UP主」；搜索框输入后命中 1 张卡且副标题变 `1 / 14 个视频`、焦点仍在输入框；排序改「播放量」后首张卡为播放量最高的那条；滚动 700px 后标签栏停在 `y=68`（顶栏下 10px）而标题已滚走（`y=-514`）；浅色/深色各出图确认无回归。

- **播放页：只有视频是黑的，页面跟随主题**（这一条中途返工过一次，留档说明）。
  - 涉及文件：`public/styles.css`
  - 走过的弯路：先按"播放器一律深色"做了一版 —— 在 `#playerView` 作用域里换掉整套令牌，让它成为一块 `#0a0a0c` 的深色舞台（圆角 18px）。**在深色主题下没问题，在浅色主题下就是"页面中间浮着一块黑板"**，用户一眼就看出不对。教训："播放器深色"指的是**视频本身**（它在亮房间里就该是一块黑），不是页面背景；要真正沉浸就得连应用外壳一起变，而不是往内容中间贴一块深色贴纸。
  - 现在的做法：播放页正常跟随主题（浅色页/深色页），只有播放壳是黑的 —— 18px 圆角 + 一层克制的接触/环境阴影（`0 1px 2px rgba(0,0,0,.06), 0 14px 34px rgba(0,0,0,.14)`，替掉原来那层 64px 的"大字报"阴影）。标题 26px、走 `--track-tight`；选集面板继续用"一块表面"（无描边）；当前集是强调色浅底 + 强调色文字；返回控件是填充胶囊。
  - 验证：无头 Edge 走**真实路径**（视频库点卡片进播放页）实测 —— 浅色：页面背景透明（继承 `#f2f2f7`）、标题 `rgb(28,28,30)`、播放壳 `rgb(0,0,0)`／圆角 18px；深色：页面 `rgb(0,0,0)`、标题 `rgb(255,255,255)`、返回胶囊 `rgba(120,120,128,.36)`。浅色/深色各出图确认。

- **微动效重做：按下即刻反馈、松手带弹性**（只改样式与两处 JS 的动画参数）。
  - 涉及文件：`public/styles.css`、`public/app.js`
  - 技术细节：
    - **按下与松手走不同的时间**：第一条原则是"响应不能隔夜"——反馈必须发生在按下那一刻。做法是让 `:active` **覆盖整条 `transition`**：按下用 `--dur-1`（100ms）立刻到位，松手换回元素本身的 `--dur-3` + `--ease-spring-soft` 弹回。两边同速是一条"死板"的曲线——按下去像踩空，弹起来也像被人推了一把。覆盖：`.btn`、`.icon-btn`、`.card`、`.hcard`、`.dash-tab`、卡片角标 `.card-add*` / `.card-remove`、收藏夹播放键、星级 `.star`。
    - **抬起与阴影必须同一时长、同一起点**：卡片原来是"位移 100ms + 阴影 200ms"，两者不同步时卡片看起来"发虚"。现在都是 200ms（位移走 `--ease-spring-soft`，阴影走 `--ease-out`）。
    - **hover → 按下不再跳一下**：`.card:active` 原来只写 `scale(0.99)`，而 hover 是 `translateY(-2px)`；两串 transform 函数不一致时会先弹回原位再缩。现在基数 / hover / 按下都写同一串 `translateY(...) scale(...)`，逐项插值。
    - **"拿起"带生长**：拖动排序时被拖的标签改用**独立的 `scale` 属性**放大 3%——`transform` 仍然每帧 1:1 跟着指针，两者互不干扰，于是拿起有 150ms 的生长而不是硬跳。
    - **落位承接手感**：松手后的 FLIP 从 `cubic-bezier` 换成弹簧曲线 `linear(0, .55 12%, .96 30%, 1.02 45%, 1)`（260ms）。这次位移是"手扔出去的"，承接它的应该是弹簧——只在手势本身带惯性时才给一点回弹。
    - **去掉整页滤镜**：弹窗打开时给 `#app` 加的 `filter: saturate(.85)` 会让整棵子树在过渡期间每帧重新栅格化（一整页重绘），而视觉收益只有"降一点饱和"。压后景由遮罩 + `scale(.992)` 已经成立，滤镜删除。
    - `prefers-reduced-motion` 下：新增的按压缩放一律 `transform: none`；拖拽落位不补间、直接就位；**拖动本身仍保持 1:1 跟手**（那是直接操作，不是装饰动效）。
  - 验证（无头 Edge、真实鼠标事件逐项读数）：① 按钮按下 `matrix(0.97)` 且 `transition-duration: 0.1s`，松手 `0.2s` + 弹簧曲线；② 松手回弹中途采样 `0.9987 → 1.00023（过冲）→ 1`，确认是过渡而不是瞬跳；③ 卡片按下 `scale(0.99)`，按下 / 松手分别走 `0.1s` / `0.2s`（悬浮形态后来改为 `scale(1.012)` 且阴影不动，见上方"悬浮不再发光"）；④ 拖动中标签 `transform` 精确等于指针位移（120px→120px）且 `transition-property` 只有 `scale`；⑤ 落位动画 `easing: linear(0 0%, 0.55 12%, 0.96 30%, 1.02 45%, 1 100%)`、`duration: 260ms`；⑥ 拖拽回归 4 项（8px 位移不换位 / Esc 取消 / 靠边自动滚动不跑飞 / 空闲 1.5s 内应用自身 rAF 调用 0 次）全过。

- **卡片悬浮不再放大封面**：上一版用"封面在图框内 `scale(1.04)` + 阴影加深"表达悬浮，实际观感是"图片在框里爬"。现在封面只做裁切、不再有自己的过渡与缩放，悬浮反馈交给卡片本身（最终形态见上方"悬浮不再发光"一条）。
  - 涉及文件：`public/styles.css`
  - 验证：hover 实测封面 `transform: none`（改前是 `matrix(1.04,...)`）。

- **「继续学习」恢复成卡片**：上一版为了"主视觉"把续播项做成了"文字直接压在画面上"的图墙（`.hcard-body` 绝对定位盖在封面上、封面自带圆角与阴影）。这丢掉了卡片最值钱的部分——**把相关的东西框在一起**：标题、进度、时间与"继续学习"角标各漂在画面上，扫视时读不出边界。现在续播与视频库共用同一种卡片形态（一块表面 + 画面 + 下方文字区），只是更大（1/2/3 列）。
  - 涉及文件：`public/styles.css`
  - 技术细节：`.hcard` 恢复 `--surface` + 圆角 + 阴影 + 内高光 + `overflow: hidden`；`.hcard-body` 由绝对定位改回静态内边距 `13px 15px 15px`；`.hcard-cover` 不再自带圆角与阴影（交给卡片裁切）。

- **播放页视觉重构**：视频区从"满宽黑块"改为圆角 20 + 更深投影的"播放壳"；返回按钮由纯文字改为填充小胶囊；标题 26px（走 `--track-tight` 字距）；右侧选集面板去掉描边改为一块表面，选集行去边框、hover 用 `--fill`，当前集用蓝色浅底 + 强调色文字。
  - 涉及文件：`public/styles.css`
  - 验证：无头 Edge 出图确认（返回胶囊 / 大圆角播放壳 / 26px 标题 / 无边框选集卡片 / 当前集蓝色浅底）。

- **动效与交互规范底座**：把散落的时长/缓动收敛成令牌，补上键盘焦点与减弱动效支持。纯样式 + 少量 JS，功能逻辑不变。
  - 涉及文件：`public/styles.css`、`public/app.js`
  - 技术细节：
    - 新增动效令牌：4 档时长 `--dur-1..4`（100 / 150 / 200 / 250ms，**按"意图"命名而不是按数值**）+ 3 条缓动 `--ease-out`（`cubic-bezier(.23,1,.32,1)`）/ `--ease-in-out` / `--ease-state`。26 处 `transition`/`animation` 声明由 0.1/0.12/0.15/0.18/0.2/0.25s 六种硬编码值改为引用令牌——六种时长混用正是"CSS 默认感"的来源；
    - 按压反馈：`.btn` / `.icon-btn` 由 `translateY(1px)` 改为 `scale(0.97)` / `scale(0.94)`。缩放会连文字和图标一起缩，读起来才像"按下去"；
    - **去掉卡片统一上浮**：`.card:hover` / `.hcard:hover` 不再 `translateY(-2px)`，只保留边框与阴影强调——一屏卡片全都在动，动效就不再表达任何具体信息。按下反馈交给 `:active` 的 `scale(0.995)`；
    - **弹窗进出对称**：遮罩与弹窗改用 `@starting-style` 淡入；`closeModal()` 先打 `data-closing` 让两者一起反向补间，等 `transitionend`（兜底 300ms）再移除节点。此前 `closeModal()` 直接清空 `innerHTML`，弹窗是"啪"地消失、遮罩连进场都没有；
    - 提示条改用 `ease` + 250ms（比弹窗略慢），与弹窗的干脆形成层次；
    - **键盘焦点**：补统一的 `:focus-visible` 焦点环（此前整份样式表只有 1 处），覆盖按钮、标签页、卡片、选集行、下拉等。卡片本来就有 `role="button" tabindex="0"`，缺的只是可见焦点；
    - **减弱动效**：`prefers-reduced-motion: reduce` 下移除位移/缩放，保留透明度、颜色与边框变化（"变温和"而不是"变没有"）；
    - hover 门控：带位移/缩放的 hover 加 `@media (hover: hover) and (pointer: fine)`，避免触屏点按误触发并"粘住"；
    - 排版：标题字距改用相对单位令牌 `--track-tight`（字号越大越收紧）；品牌标签去掉边框、背景压淡一档，退到第二层；
    - 清理：删除两个已无引用的 `@keyframes`（`fade-in` / `pop-in`）。
  - 验证：无头 Edge **真实时间**实测——弹窗进场 `opacity 0 → 1`（`enter_animated=true`）、退场中途采样 `0.066`、400ms 后节点被移除；令牌均正确解析；CSSOM 中 `.card:hover` 已不含 `transform`。改前改后首屏截图基本一致（658.7KB → 658.2KB），确认没有视觉回归。
  - ⚠️ 踩坑留档：**不要用 `--virtual-time-budget` 验证过渡动画**。虚拟时间不推进合成器动画，会把正常的进场误判成"卡在 opacity 0"；第一轮据此以为弹窗坏了，换真实时间才看到正确的 `0 → 1`。

- **视觉与材质改造一轮**（功能与逻辑不变，改动集中在样式与少量渲染代码）。
  - 涉及文件：`public/styles.css`、`public/app.js`、`docs/img/dashboard.png`
  - 技术细节：
    - **标签栏终于有层级了**：`.dash-tab` 原本写 `color: var(--muted)`，而这个变量**从未定义过**——无效值让 `color` 退回"继承"，未选中的标签和选中标签一样深。改用 `var(--text-2)`，实测未选中 `rgb(110,110,115)` / 选中 `rgb(47,111,237)`；
    - **活动标签换成"会滑动"的指示条**：以前每个标签各画一条 2px 下划线、切换时硬切；现在只有一条 `.dash-tab-ind`，位移与宽度都走 `transform`（切页时滑过去），切到自定义标签时自动变绿。位置用 `getBoundingClientRect` 计算（天然包含横向滚动位移），在渲染后 / 标签区滚动 / 窗口 resize 时同步；
    - 踩坑留档：`scaleX()` 只接受**无单位**数字，第一版写成 `scaleX(114px)` 会让整条声明失效，表现就是"指示条根本不出现"——是程序化验证抓出来的，肉眼看不出来；
    - **顶栏改成滚动边缘**：分隔线默认透明，滚动超过 4px 才浮现（悬浮玻璃不该被一条常驻硬线说成"固定的条"）。实测 顶部 `rgba(0,0,0,0)` → 滚动后 `rgb(230,230,225)`；
    - **遮罩"材质化"**：`backdrop-filter` 的模糊半径与透明度一起推出来（`blur(3px) saturate(1.15)`），不再是单纯变深；
    - **弹窗更"厚"**：新增 `--shadow-modal`（比卡片更重的一层阴影）+ 上沿一道极淡高光（光打在材质上）；
    - **排版落地**：补 `--fs-md`，把此前定义却没用上的字号令牌真正应用到标题 / 卡片 / 标签 / 按钮；`font-optical-sizing: auto`；卡片的日期与时长用 `tabular-nums` 对齐；
    - **另外两条无障碍信号**：新增 `prefers-reduced-transparency`（玻璃改实心、去模糊）与 `prefers-contrast: more`（边框与次级文字加实）；减弱动效下指示条不再滑行、直接就位；
    - README 里的主页截图同步换成新版（`docs/img/dashboard.png`）。
  - 验证（无头 Edge，顶层页面实测）：指示条矩形与目标标签误差 ≤1.5px；切到自定义标签后变绿且仍对齐；顶栏分隔线随滚动出现、回到顶部消失；标签层级生效；浅色 / 深色各出一张图确认无回归。
  - 说明：这一轮**刻意没有**动布局结构与主色——先把层级、材质与反馈做对，再谈装饰。

- **视觉再进一步：环境光 / 卡片材质 / 续播主视觉 / 首屏入场**（功能与逻辑不变）。
  - 涉及文件：`public/styles.css`、`public/app.js`、`docs/img/dashboard.png`
  - 技术细节：
    - **顶部环境光**：`body::before` 铺一层只用强调色单一色相的径向渐变（多色渐变是最典型的"AI 味"来源），让界面有"从上方照下来"的光源；深色模式自动减弱（`--glow`）；
    - **卡片材质**：阴影改成两层（近处接触阴影 + 远处大范围阴影）＋上沿一道高光（`--card-inner`，光打在材质上）；封面底部压一层很淡的暗角，让时长胶囊与角标"坐得住"；
    - **悬浮反馈改为"封面推近"**：卡片不再整体位移，改为封面在图框内 `scale(1.04)`，配合阴影加深；
    - **「继续学习」主视觉化**：从 `auto-fill minmax(300px)` 改为固定 1 / 2 / 3 列（720px / 1500px 断点），并加 `align-items: start`——原先让第一张跨两列会一大一小、右边留一大块空白，且同一行的小卡会被高卡拉长成空壳；
    - **进度条**：4px→5px、圆角胶囊、蓝色渐变 + 一点辉光；图标角标从实心强调色改为半透明深色 + 模糊，与右下角时长胶囊统一材质；
    - **首屏入场**：卡片 28ms 递进（`opacity` + `translateY`，整体 ≤450ms），进度条从左"画"出来；**只在页面首次渲染时播一次**（切标签、输入搜索都会重跑渲染，每次都播就成了闪屏），动画结束由 JS 摘掉 `.first-paint`，避免 animation 的 fill 盖住卡片自己的 hover/:active transform；
    - **弹窗打开时页面后退**：`body:has(#modalRoot:not(:empty)) #app { transform: scale(.992); filter: saturate(.85) }`——焦点自然落到弹窗上（`#modalRoot` 在 `#app` 之外，所以给 `#app` 加 transform 不影响弹窗/提示条的 fixed 定位）；
    - 搜索框与排序控件改为"填充式"（控件自己有底色，聚焦时才浮起 + 焦点环），搜索框加左侧放大镜；
    - `＋`（新建标签页）按钮原本用不透明的 `--bg` 做底——有了环境光之后，它在光里显出一块"洞"（实测读到的正是 `rgb(247,247,245)`）；改为全透明 + `backdrop-filter: blur(8px)`，既挡住滑过的标签又与光融为一体。
  - **顺手修掉一个只在深色模式出现的 bug**：续播卡片的标题在深色下不显示。`.hcard-body h3` 用了 `white-space: nowrap + overflow: hidden + text-overflow: ellipsis` 的组合；换成与视频库卡片一致的 `-webkit-line-clamp: 2`（并显式声明 `color: var(--text)`）后恢复，长标题也不再被切成一行省略号。
  - 验证：浅色 / 深色各出整图与原生分辨率局部图；对标题做过 `elementFromPoint`（无遮挡）、颜色/尺寸/裁剪三项探针；确认无遮挡与样式无异常后才定位到裁剪属性。
  - ⚠️ 截图踩坑：`--virtual-time-budget` 下 CSS 动画不推进，带 `fill: both` 的入场动画会让卡片停在 `opacity: 0`（截图一片空 ✗）。拍"终态"图要用 `--force-prefers-reduced-motion`（该模式下动画被关闭）。

- **视觉层重做：分段控件 / 无卡片的图墙 / 文字压在画面上**（数据与交互逻辑不动，改的是渲染出来的结构与样式）。
  - 涉及文件：`public/styles.css`、`public/app.js`、`docs/img/dashboard.png`
  - 技术细节：
    - **标签栏改成分段控件**（切换视图的标准控件）：一个填充的圆角容器 + 一块会滑的白色滑块，宽度按内容收缩、不再整行铺开；滑块跟着选中的那一段走（位移 + 宽度，弹簧曲线 `--ease-spring`），自定义标签的滑块带一点绿以保留"源 / 库"的语义；选中态用"文字变成满对比度"表达，颜色标记交给滑块。滑块几何由 `syncTabIndicator()` 输出 `translateX + width`（不再用 `scaleX` 拉伸，那会把圆角拉变形）；
    - **网格去掉"卡片"**：`.card` 不再有描边、底色和卡片阴影，画面本身成为内容、文字直接坐在页面底色上；层次由画面自己的阴影承担；悬浮时"画面浮起来"（阴影加深 + 画面推近 `scale(1.04)`，用 `--ease-spring-soft`），卡片本身不位移；
    - **续播卡把文字压在画面上**（压在画面上的做法）：标题、进度、时间与"继续学习 · 剧集"角标都坐在画面里，底部一条渐变保证可读；进度条改成细白线（比彩色渐变更像系统组件）；点击仍然交给整张卡（文字层 `pointer-events: none`）；
    - **删除按钮挪到画面上**：视频库卡片的 ✕ 从文字行移到画面右上角，与「+ / ✓」和续播卡统一成同一种半透明材质（`rgba(15,15,20,.5)` + `backdrop-filter`）；
    - 栏目标题放大到 21px 并收紧字距；弹簧曲线用采样点写成（`linear(...)`），纯 CSS 就有回弹手感，不引动画库。
  - 验证：浅色 / 深色、续播 / 视频库四种组合各出一张整图确认；README 顶部的截图同步换成新版。

- **重做配色，并把卡片恢复成"表面"**（功能与逻辑不变）。
  - 涉及文件：`public/styles.css`、`public/app.js`、`docs/img/dashboard.png`
  - 技术细节：
    - **配色换成系统的语义色**：页面底 `systemGroupedBackground`(#f2f2f7) + 卡片 `systemBackground`(#fff)——"白卡片浮在浅灰底上"正是分组列表 / 网格的经典形态；文字只留两级（label #1c1c1e / secondaryLabel #8e8e93），层级靠字重与字号而不是多套颜色；强调色换成系统蓝（浅色 #0071e3 / 深色 #0a84ff）；
    - **分隔线与控件填充改为半透明**：`--separator: rgba(60,60,67,.13)`、`--fill: rgba(120,120,128,.12)`（系统的 separator / systemFill），并把 `--border` 指向 `--separator`——于是所有既有的 `1px solid var(--border)` 自动变成能适配任何底色的半透明分隔；搜索框、排序、表单控件改用 `--fill` 填充；
    - **深色是另一套调色板**（不是把浅色反过来）：底改纯黑、层次往上"变亮"（#000 → #1c1c1e → #2c2c2e），强调色与绿色用深色模式专用的更亮取值；
    - **卡片恢复为"一块表面"**：卡片本来就是"把相关的东西框在一起"的容器，价值必须保留；做法是**去掉描边**、用「圆角 + 底色」表达边界、用阴影表达层次（静置一层接触阴影，悬浮加深），画面贴到卡片边缘由圆角裁切；卡片内部四周统一的 13–14px 内边距让"框在一起"读得出来；
    - **标签栏改成"浮动玻璃"**：分段控件加 `position: sticky`（吸在顶栏下方），背景改为半透明 + `backdrop-filter` 模糊，滚动时它浮在内容之上而不随页面滚走（滚动的悬浮层）；`prefers-reduced-transparency` 下退化为实心。
  - **修一个我引入的回归**：改成分段控件后，拖拽排序会"卡死"。原因是标签拖拽靠近边缘时的自动滚动循环**只在滚动区确实溢出时才该跑**；分段控件容器是 `fit-content`，标签不多时根本不溢出，于是那个 `requestAnimationFrame` 循环以 60fps 空转，看起来就是整页卡住。修法：起循环前先判 `scrollWidth > clientWidth`，并且滚到边界（`scrollLeft` 不再变化）时立即停止。

## [1.2.2] - 2026-09-12

### Fixed（修复）

- **卡片上的「添加于」显示成"5 万多年"**：`fmtDate()` 一直按「秒」解释时间戳，但时间戳其实有两种来源——B 站接口给的 `fav_time` 是**秒**，本地添加视频存的 `addedAt` 是 `Date.now()`（**毫秒**）。于是自己添加/收藏的视频，卡片上会显示成「添加于 58667年6月29日」。
  - 涉及文件：`public/app.js`
  - 技术细节：`fmtDate()` 里补一句毫秒换算（`if (n > 1e12) n = Math.round(n / 1000)`）；**不改动已存数据**，也不影响排序（排序比较的是原始值，同单位之间大小关系一致）。

### Changed（变更）

- **README 顶部加主页截图**：`docs/img/dashboard.png`（1600×880），并给仓库主页填上了 Homepage（指向 Releases 页）。截图为**演示数据**——标题是合成的、封面是程序生成的抽象图，不含任何人的真实学习列表。
  - 涉及文件：`README.md`、`docs/img/dashboard.png`
  - 技术细节：截图由无头 Edge 在临时实例（4176 端口 + 临时数据目录 + 全新浏览器配置）上以 2× 缩放拍摄后降采样得到；顺带用这张图验证了上面的日期修复。

## [1.2.1] - 2026-09-12

### Changed（变更）

- **合规表述如实化 + 补齐第三方许可**：把 README、首次启动引导、设置 →「关于」里与实现不符的措辞改成如实描述。纯文案与文档改动，不动任何功能逻辑。
  - 涉及文件：`README.md`、`public/app.js`、`public/vendor/DASH_LICENSE`、`installer/bilinest.iss`、`package.json`
  - 技术细节：
    - 原文写"也不提供任何破解、绕过付费或**反爬**能力"，但代码里实现了 WBI 签名（`wbiSignQuery`）与 CDN 所需的 Referer 转发——声明与事实不符，反而可能被用来证明主观故意。现改为如实说明：复用 B 站网页端同款**只读**接口与网页端公开使用的签名参数，这是"在本机播放自己账号有权观看的内容"所必需；同时明确列出**不做**的事（不破解付费/会员、不绕过账号权限、不下载、不批量抓取、不去水印、不绕地区限制）；
    - README 顶部新增**非官方声明**（中英文各一），明确与哔哩哔哩无隶属、合作或授权关系，也不使用其商标与品牌标识；
    - 新增 FAQ「这样做会违反 B 站用户协议吗？」，如实回答第三方客户端通常不符合平台协议，并写明项目边界与责任归属；
    - 第三方许可：新增 `public/vendor/DASH_LICENSE`（dash.js 的 BSD-3-Clause 全文，取自上游 `LICENSE.md`，措辞未改动）；README 许可表补上此前**漏列**的 `artplayer-plugin-danmuku`，并把 dash.js 的指向从"文件头"（实测该文件头部并没有许可证横幅）改为 `vendor/DASH_LICENSE`；README 同时注明安装包用的 Inno Setup 中文语言文件来源；
    - 凭据提示：README 与设置 →「数据」都注明 `state-backup.json` 里是**明文** SESSDATA，不要分享 / 同步 / 上传；「关于」写明本地服务只监听 `127.0.0.1`。

## [1.2.0] - 2026-09-12

### Added（新增）

- **Windows 安装包**：新增 `installer\`，用 Inno Setup 6 编译出 `dist\BiliNest-<版本>-Setup.exe`（约 2.3MB）。向导为简体中文，默认装到 `%LOCALAPPDATA%\Programs\BiliNest`（**不需要管理员权限**，安装位置可改），自动创建桌面与开始菜单快捷方式、在「应用和功能」登记卸载项；卸载时先停掉正在跑的本地服务，再询问是否连用户数据一起删（默认保留）。
  - 涉及文件：`installer\bilinest.iss`、`installer\build.ps1`、`installer\languages\ChineseSimplified.isl`、`.gitignore`、`README.md`
  - 技术细节：
    - Node.js **不自带**（不为一个运行时白胖 87MB）：按「PATH → `%ProgramW6432%\nodejs` → `%ProgramFiles%\nodejs` → `%ProgramFiles(x86)%\nodejs` → `%LOCALAPPDATA%\Programs\nodejs`」逐个探测；缺失或低于 18 时出现「运行环境检查」页，三选一：winget 自动安装 / 打开 nodejs.org / 先跳过。探测结果会写进安装日志（`/LOG=` 可查），Ready 页也会显示最终状态；
    - **踩坑留档（第一版误报"没装 Node"的根因）**：探测最初写成 `Exec('{cmd}', '/C "node" -v > "out.txt"')`——cmd 在 `/C` 之后同时遇到引号包裹的路径和 `>` 重定向时会把最外层引号剥掉，于是 `C:\Program Files\nodejs\node.exe` 这类带空格的路径全部执行失败。改成先写出一个临时 `.bat` 再执行，彻底绕开引号规则；
    - 另外两个 Inno 的坑：`[Code]` 里以 `[` 开头的续行会被当成新段标签（`Format(..., [` 必须写在同一行）；Pascal Script 不允许前向引用，函数必须先定义后使用；
    - 编译需要 Inno Setup 6（`winget install JRSoftware.InnoSetup`，约 5MB）；简体中文语言包 Inno 不自带，取自官方源码仓库 `Files/Languages/Unofficial/ChineseSimplified.isl`，放在 `installer\languages\`；
    - `build.ps1` 会校验 `bilinest.iss` 的 `AppVersion` 与 `package.json` 的 `version` 一致，避免发出对不上号的包；
    - 实测：安装后 26 个文件、桌面/开始菜单快捷方式与卸载项齐全，装出来的文件与仓库**逐字节一致**（SHA256 比对）；`/VERYSILENT` 安装与卸载都能跑通，卸载后目录、快捷方式、注册表项全部清空、用户数据按默认保留。

- **没装 Node.js 时给出明确指引**：`launcher.vbs` 启动服务前先确认 Node 可用，找不到就打开 `public\setup-help.html`（中文说明页：winget 命令、官网下载链接、PATH 注意事项），而不是像以前那样静默失败、让用户对着一片打不开的页面发愣。
  - 涉及文件：`launcher.vbs`、`public\setup-help.html`
  - 技术细节：`ResolveNode()` 先试 PATH 上的 `node`，再依次试 `%ProgramFiles%\nodejs`、`%ProgramFiles(x86)%\nodejs`、`%LOCALAPPDATA%\Programs\nodejs`，命中后返回可执行命令（带空格的路径会加引号）；中文文案放在 HTML 里，`launcher.vbs` 保持**纯 ASCII + 无 BOM**（WSH 只认 UTF-16 的 BOM，UTF-8 BOM 会报 800A0408）。

- **状态备份 / 自动恢复**：客户端状态会防抖上传到本地服务，存成 `%APPDATA%\BiliNest\state-backup.json`；本地没有真实数据时（清过浏览器数据、换了浏览器、换过端口）自动恢复并刷新一次。定位是"清一次浏览器数据就全没了"的防丢兜底。
  - 涉及文件：`server.mjs`、`public/storage.js`、`public/app.js`
  - 技术细节：
    - `POST /api/state/backup` 写入（先写 `.tmp` 再改名，原子替换）、`GET` 读回、`POST {"clear":true}` 删除；请求体限长 8MB；只接受 `state.v === 1` 的对象，其余返回 400；
    - **只允许同源访问**：校验 Host 必须是 `127.0.0.1` / `localhost`，且请求若带 `Origin` 必须与 Host 一致，否则 403。备份里含 SESSDATA，不能像其它只读接口那样开放跨域，否则用户访问的任意网页都能把 Cookie 读走；
    - 客户端 `save()` 之后防抖 1.5s 上传一次（连续改动只传一次）；`store.restoreIfNeeded()` 在"本地无状态 + 服务端有备份"时写入 localStorage 并让应用 `location.reload()` 一次；应用启动时另有一次 `backupNow()`，保证在原环境打开一次就把备份建好；
    - 新增 `hasRealData()` 守卫：登录态 / 视频库 / 收藏夹库 / 学习 UP主 / 自定义标签页 / 观看记录 任一非空才算"有数据"，**空状态不上传、也不当作可恢复的备份**（否则空壳状态会把好备份写脏）；
    - 「清除全部本地数据」会同时删除服务端备份（否则刷新就被恢复回来）；设置 →「数据」另有「从备份恢复」按钮可手动触发；
    - 服务端数据目录：Windows `%APPDATA%\BiliNest\`，其他平台 `~/.config/BiliNest/`，可用环境变量 `BILINEST_DATA_DIR` 覆盖。**刻意不放安装目录**：装到 Program Files 时普通用户没有写权限，卸载也会被一并删掉；
    - 除只读接口外，这是本服务唯一的写接口（`POST_PATHS` 白名单，其余非 GET/HEAD 仍返回 405）；
    - **不在备份范围内**：本地视频的文件句柄存在 IndexedDB 里，无法序列化成 JSON，换环境后需要重新添加本地文件。

- **备份升级为"跨环境的权威副本"：两边都有数据时以较新的一份为准**。以前只有"本地为空"才会恢复，于是换浏览器时容易各存一份、并且长时间没用过的那份可能反过来把较新的备份覆盖掉；现在会按时间戳自动收敛到较新的那份。
  - 涉及文件：`public/storage.js`、`server.mjs`、`public/app.js`
  - 技术细节：
    - 状态里新增 `updatedAt`（毫秒），由 `save()` 在每次真实改动时写入；旧数据没有该字段时按 `0` 处理；
    - 启动时 `restoreIfNeeded()` 的判定改为：本地没有真实数据 → 直接取备份；本地也有数据 → 只在**备份的 `updatedAt` 更大**时覆盖本地，否则以本地为准并照常上传。**刻意不用服务端的 `savedAt` 比较**——它总比改动晚一点点，会让每次启动都误判成"备份更新"而反复恢复刷新；
    - 上传侧加了一道保险：`POST /api/state/backup` 在两边都带时间戳、且传入的更旧时，保留现有备份并返回 `{ kept: 'newer' }`，防止旧快照覆盖新数据；任一侧时间戳缺失（旧版本数据）一律放行，保持宽容；
    - 「从备份恢复」按钮仍是**强制**覆盖（不比较时间戳），用于数据被改坏时手动回滚。

### Changed（变更）

- **设置弹窗改为「左侧栏位 + 右侧内容」**：原来一长条纵向堆叠的设置项拆成四个分类（登录与授权 / 外观 / 数据 / 关于），左栏切换、右栏只显示当前分类，并记住上次打开的是哪一栏。
  - 涉及文件：`public/app.js`、`public/styles.css`、`public/storage.js`
  - 技术细节：
    - 所有原有控件 id 保持不变（`btnQrLogin` / `btnSaveCookie` / `btnClearAuth` / `themeSelect` / `btnClearData` / `btnShutdown` / `btnGuide` 等），`bindSettingsEvents()` 除新增左栏绑定外无需改动，隐藏面板里的控件也照常可绑定；
    - `.modal-body.settings-body` 用 flex 分成左右两栏，右栏独立滚动；`openModal()` 新增可选 `cls` 参数，设置弹窗宽 780px（`.modal-settings`）；
    - 当前栏位存进 localStorage 的 `settingsTab`（默认 `login`，非法值回落 `login`）。

### Other（其他）

- **路线决定：放弃"独立窗口（`--app`）/ 桌面外壳"，回到双击快捷方式用默认浏览器打开标签页**。`launcher.vbs` 回退为只做「起服务 → 打开 `http://127.0.0.1:<实际端口>`」，随之移除 `/api/prefs`（窗口模式偏好）、设置里的「启动方式」选项与 Edge / Chrome 探测；WebView2 外壳（`shell/`）也不再作为可选入口。
  - 涉及文件：`launcher.vbs`、`create-shortcut.ps1`、`server.mjs`、`public/api.js`、`public/app.js`、`.gitignore`
  - 理由：这条路线换来的只有"窗口没有地址栏 / 尺寸可控"，代价却是每次都要多起一套浏览器进程树，而这两件事都不值（见下条实测）。

- **核查记录：启动 BiliNest 会带起多少浏览器进程**（留档，避免以后重复调研）。
  - 实测（2560x1440 / 125% / 本机 Edge，私有内存）：**同一个空白页**在一个全新浏览器配置目录下就是 **15 个进程**；换成 BiliNest 页面仍是 15 个（约 540MB）；加上 `--disable-component-extensions-with-background-pages --disable-extensions` 后降到 10 个（约 382MB）。
  - 构成：浏览器主进程 / GPU / Network Service / Storage Service / Extractor / CollectionsDataManager / Crashpad / 备用呈现器（约 9 个）+ Edge **自带的**约 6 个扩展宿主（Copilot / 收藏集 / 购物助手那一批，任何配置目录都会加载，与用户装没装扩展无关）。BiliNest 自己的增量只有一个页面渲染进程（约 35–75MB）和一个隐藏的 `node` 进程（约 40–60MB）。
  - 下限就是如此：Chromium 系宿主一个窗口就是 8–10 个进程、几百 MB；想明显更省只能换非浏览器宿主，而 WebView2 外壳试过，又因数据目录隔离与冷启动慢被否掉。
  - 顺带查清的两条浏览器机制（以后少走弯路）：① `--window-size` / `--window-position` **只在浏览器新起进程、且该配置目录没有记录过窗口位置时生效**——Edge 即使关掉所有窗口也会留后台进程，此时参数被直接丢弃，窗口沿用记忆值；② `--window-size` 收的是 CSS 像素，WMI 报的是物理像素，中间差一个显示缩放比（本机 125%），不换算会算出越界尺寸被系统丢弃。
  - 反例留档：往 Edge 的 `browser.app_window_placement` 里手写尺寸确实能改变窗口，但坐标系与缩放换算难以稳定复现（同一台机器先后出现过 ×1.25 与 ×1.8 两种结果），所以没有采用"改浏览器记录"这条路线。

## [1.1.0] - 2026-09-12

### Added（新增）

- **学习 UP主书签**：主页新增「学习 UP主」栏，输入 UID 添加 UP主 卡片（头像/名称/简介/粉丝数/星级评分），点击卡片跳转 B站个人主页。
  - 涉及文件：`server.mjs`、`public/storage.js`、`public/api.js`、`public/app.js`、`public/styles.css`
  - 技术细节：
    - 新增 `/x/web-interface/card` 代理路由获取 UP主 信息；
    - `studyUps` 存储于 localStorage，结构 `{ mid, name, face, sign, fans, videos, level, addedAt, stars }`；
    - 卡片点击 `window.open('https://space.bilibili.com/{mid}', '_blank')`；
    - 补充 `fmtCount`、`fixAvatar` 工具函数。

- **主页标签页**：主页从纵向堆叠改为标签页切换模式（继续学习 / 视频库 / 收藏夹库 / 学习 UP主），每次只显示一个栏位，标签状态持久化。
  - 涉及文件：`public/app.js`、`public/styles.css`、`public/storage.js`
  - 技术细节：
    - `renderDashboard()` 重写为标签栏 + 内容区两层结构；
    - `state.activeDashTab` 持久化到 localStorage，记住上次选中的标签；
    - 每个标签独占整个主页空间，默认显示更多卡片（继续学习 12 / 视频库 20 / 收藏夹 12 / UP主 12）；
    - 搜索框仅在「视频库」标签下显示。

- **移除学习 UP主**：学习 UP主 卡片右侧新增「✕」按钮，点击后弹出确认框，确认即从列表中移除。
  - 涉及文件：`public/app.js`
  - 技术细节：复用 `confirmAction()`（与「从收藏夹库移除」一致）；卡片渲染新增 `data-up-remove`，`onDashboardClick` 中在卡片跳转之前处理并 `stopPropagation`，避免误触发跳转 B站主页。

- **空状态补充「添加 UP主」入口**：仪表盘没有任何内容时（包括移除最后一个 UP主 之后），空状态在「打开内容源」旁新增「添加 UP主」按钮，避免进入再也加不了 UP主 的死角。
  - 涉及文件：`public/app.js`

- **自定义标签页**：标签栏末尾新增「＋」直接新建标签页（建完立即内联改名，双击标签也能改名；悬浮标签出现的「⋯」可重命名 / 删除标签页）。空标签页内有一张「＋ 添加内容」卡片，点开是四分区选择器：源收藏夹 / 视频库 / 收藏夹库 / 学习 UP主；库侧三个分区的条目带缩略图（封面 / 头像，无图时显示占位文字），源收藏夹列表不显示封面。选视频不再另做一套浏览界面——点「源收藏夹」里的收藏夹会直接进入右上角「内容源」那套收藏夹视图（自带搜索 / 排序 / 分页），只是带着「正在往哪个标签页加」的上下文。
  - 涉及文件：`public/storage.js`、`public/app.js`、`public/styles.css`
  - 技术细节：
    - `customTabs` 存于 localStorage，结构 `{ id, name, createdAt, items:[{ kind:'video'|'folder'|'up', id }] }`；`items` 只存库内实体的引用，不拷贝数据，删标签页不会删内容；
    - `tabMembers()` 解析成员时跳过已失效的引用（条目从库中删除后自愈）；成员按 视频 / 收藏夹库 / 学习 UP主 分组渲染，复用 `videoCard()` / `folderCard()` / `studyUpCard()`（三者新增可选 `ctx` 参数：`ctx.tab` 存在时卡片上的 ✕ 变为「从本标签页移除」，不动库）；
    - 选择器里「源收藏夹」分区勾选收藏夹本体 → 先写入 `studyFolders`（入库）再入页；点收藏夹行则关闭选择器、进入收藏夹视图（复用内容源那一套），并设置 `state.pendingTabId` 记录目标标签页；另外三个分区只入页（内容已在库）；已在本页的条目置灰、不可重复勾选；
    - 库侧三个分区支持排序：视频「星级 / 添加时间 / 播放量 / 发布时间」、收藏夹库「星级 / 添加时间 / 视频数」、UP主「星级 / 添加时间 / 粉丝数」，默认星级优先（次键为添加时间）；
    - 源收藏夹列表不显示封面：B站收藏夹列表接口本身不返回封面，显示占位图没有辨识度（`pickerRows()` 里给该分区行标记 `plain: true`，渲染时跳过缩略图）；
    - 新增 `ensureVideoInLibrary(bvid, media)`：把收藏夹里的单个视频补进「视频库」并返回库内 id，与内容源里的「加入学习」共用同一段「多P / 合集」归类逻辑；提交时逐条 await，失败的条目会单独计数并提示；
    - 主页标签栏改为数据驱动（`SYSTEM_TABS` + `customTabs()`）；新增 `normalizeDashTab()`，标签页被删或存储值非法时回落到「继续学习」；
    - 自定义标签页的搜索关键字各自记录（`state.tabQuery`，内存态，不持久化），不会与其他标签页串台。

- **收藏夹视图的卡片右上角状态区**：同一张视频卡片按来源渲染不同状态，一眼看出「在不在库里」和「在不在某个自定义标签页」。
  - 涉及文件：`public/app.js`、`public/styles.css`
  - 技术细节：
    - 蓝色 ✓ = 已在「学习列表」（库）；绿色 ✓ = 已在自定义标签页（从标签页进入时指当前页，从内容源进入时指任意标签页，悬浮显示标签页名）；
    - 从自定义标签页进来（`state.pendingTabId` 有值）时，未入库的视频显示一个**淡绿色 +**，悬浮提示「添加到「xx」标签页（同时加入学习列表）」，点一下同时完成入库 + 入页，随后变为蓝 ✓ + 绿 ✓；
    - 从右上角「内容源」进来（无上下文）时保留原有蓝色 +/✓，另加一个「添加到」按钮：点击弹出标签页菜单（已有标签页 + 「新建标签页并加入」），选完自动补上绿色 ✓；
    - 收藏夹视图头部在有上下文时显示「正在添加到「xx」标签页」；回主页即清除该上下文；
    - `addVideoToTab()` 复用 `ensureVideoInLibrary()` 完成「先入库再入页」；通用小面板 `openActionMenu()` 由标签页「⋯」菜单与本次的标签页选择菜单共用。

- **新标签页默认名自动编号**：连续新建时依次叫「新标签页」「新标签页2」「新标签页3」…；某个默认名被改掉后，该序号会重新空出来复用（改成「日语」后，再新建又叫「新标签页」）。
  - 涉及文件：`public/app.js`
  - 技术细节：`nextDefaultTabName()` 用现有标签页名做集合，取最小可用序号。

- **自定义标签页改成绿色强调色 + 与系统标签加分隔线**：系统标签页保持原来的蓝色，自定义标签页（选中态、悬浮态、下划线）改用绿色，与「加入标签页」的绿色 +/✓ 统一；系统标签与自定义标签之间加一条竖分隔线。
  - 涉及文件：`public/styles.css`
  - 技术细节：新增设计令牌 `--tab-green` / `--tab-green-soft`（深色模式单独取值），`.dash-tab.custom(.active/:hover)` 使用它；原来写死的绿色（`.card-flag-tab`、`.card-add-tab`）改为引用同一令牌；分隔线 `.dash-tabs-sep` 只在存在自定义标签页时渲染。

- **自定义标签页支持拖动排序**：按住标签横向拖动即可调整顺序，拖到「＋」上可移到最末。
  - 涉及文件：`public/app.js`、`public/styles.css`
  - 技术细节：自定义标签（含内部文字）标记 `draggable`；在 `els.dashboard` 上委托 `dragstart/dragover/drop/dragend`，`dragover` 时按鼠标在目标标签左/右半边显示 `.drop-before` / `.drop-after` 插入指示；落点交给 `moveCustomTab(srcId, targetId, after)` 重排 `customTabs` 数组并持久化；改名输入框上不触发拖动。

- **标签页过多时的处理：横向滚动**：系统标签固定在左侧，自定义标签放进可横向滚动的区域，标签再多也不会把「＋」挤出屏幕或撑破页面。
  - 涉及文件：`public/app.js`、`public/styles.css`
  - 技术细节：
    - 标签栏改为三段结构：系统标签（固定）+ 分隔线（固定）+ `.dash-tabs-scroll`（`flex:1; min-width:0; overflow-x:auto`，隐藏滚动条）；
    - 「＋」放在滚动区末尾并用 `position: sticky; right: 0`：**不溢出时它就是普通流式位置（跟在最后一个标签后面），只有内容溢出、横向滚动时才贴住右侧**保持可达；溢出时给一层左侧阴影说明有内容从下面滑过；
    - 滚动区向左滚出内容时左侧渐隐（`.scrolled` + `mask-image`）；鼠标滚轮在标签栏上时纵向增量转成横向滚动；
    - `syncTabsScroll()` 在每次渲染后把当前标签滚进可视区，并**预留贴住的「＋」的宽度**（不能用 `scrollIntoView`，它不知道右侧被覆盖，会把当前标签塞到「＋」底下）；窗口 resize 时重新计算；
    - 拖动排序时靠近滚动区两侧 48px 内自动滚动（rAF 循环），否则拖不到看不见的位置。

- **从标签页移除内容时可选「同时从库中删除」**：自定义标签页里卡片上的 ✕ 不再直接移除，改成二级确认框，内含一个**默认不勾选**的「同时从库中删除」复选框；勾选则连同库里的实体一起删（从「视频库」/「收藏夹库」/「学习 UP主」里删掉，其它标签页里的它也会一起消失）。
  - 涉及文件：`public/app.js`
  - 技术细节：`removeFromTab()` 改用 `confirmAction(msg, onConfirm, beforeClose)` 的 `beforeClose` 读取复选框；新增 `deleteFromLibrary(kind,id)`（不弹确认的裸删除）、`findLibraryItem()` / `libraryItemName()`、`dropMemberEverywhere(kind,id)`（清掉所有标签页里的悬空引用）；视频走 `doRemoveCustomVideo(..., silent)` 复用既有的整季删除 + 历史记录清理，并新增 `silent` 参数避免重复 toast。

### Fixed（修复）

- **主页标签页选中状态刷新后丢失**：`state.activeDashTab` 从未从 localStorage 恢复，「记住上次选中的标签」实际不生效（刷新后总是回到「继续学习」）。
  - 涉及文件：`public/app.js`
  - 技术细节：`init()` 中在 `loadDashboard()` 之前补 `state.activeDashTab = store.get('activeDashTab') || 'continue'`。

- **学习 UP主 卡片排版错误（竖排）**：卡片标记同时带有通用 `.card` 类，其 `flex-direction: column` 覆盖了 `.up-card` 的横排意图，导致头像居中在上、文字堆叠在下。
  - 涉及文件：`public/styles.css`
  - 技术细节：`.up-card` 显式补 `flex-direction: row`。

- **自定义标签页头部排版错乱**：头部用的是 `.dash-head`，但样式表里只有 `.section-head`，于是 `display` 落到默认的 `block`——标签页名和右侧计数被挤成上下两行，且搜索框底部与该行**零间距**，看起来又挤又不对齐。
  - 涉及文件：`public/app.js`、`public/styles.css`
  - 技术细节：两处 `.dash-head` 改用已有的 `.section-head`（flex + 居中 + space-between）；`.dash-search-wrap` 补 `margin-bottom: 18px`；计数改为紧跟标题的胶囊（`.tab-head` + `.tab-count`），不再被推到页面最右侧。

- **双击标签改名不生效**：第一次点击会 `renderDashboard()` 重建整个标签栏，第二次点击落在新节点上，`dblclick` 事件因此丢失。
  - 涉及文件：`public/app.js`
  - 技术细节：标签点击处理里，若点的已是当前标签则跳过重渲染（保住 DOM 节点）；同时用 `click` 的 `e.detail >= 2` 兜住第二次点击并转入改名，`.dash-tab-more`（⋯）上的双击不触发改名。

### Changed（变更）

- **主页两个栏位改名**：「添加的视频」→「视频库」，「学习收藏夹」→「收藏夹库」。同名的二级浏览页标题、内容选择器分区名、空状态提示、引导文案与各处提示语一并同步（`内容源` 弹窗内的「我的视频」保持不变，那是内容源自己的叫法）。
  - 涉及文件：`public/app.js`、`public/storage.js`、`public/index.html`、`README.md`

- **UP主 功能确定只做最小版本，完整规划作废**：本版本对 UP主 只做「本地书签」——输入 UID 添加、卡片展示头像/名称/简介/粉丝数/星级、点击卡片跳转 B站个人主页。原规划中的「搜索 UP主」「已关注列表」「站内 UP主 主页（投稿 / 合集 / 列表）」因功能过于复杂，已主动放弃，对应规划文档 `docs/feature-add-up.md` 一并删除。
  - 涉及文件：`docs/feature-add-up.md`（删除）、`CHANGELOG.md`
  - 技术细节：
    - 不新增以下代理路由：`/x/web-interface/wbi/search/type`、`/x/relation/followings`、`/x/space/upstat`、`/x/space/wbi/arc/search`、`/x/space/navnum`、`/x/polymer/web-space/seasons_series_list`、`/x/polymer/web-space/seasons_archives_list`、`/x/series/archives`；
    - 不新增以下前端 API 与视图：`searchUser` / `myFollowing` / `userUpstat` / `userVideos` / `spaceNavnum` / `seasonsSeriesList` / `seasonArchives` / `seriesArchives`、`upSpaceView`；
    - 实际只保留 `/x/web-interface/card`（`api.userCard`）一个新增接口。

## [1.0.2] - 2026-09-07

### Added（新增）

- **选集悬浮完整标题**：播放列表里长集名被省略号截断时，悬浮选集行会在其右侧显示完整集名（不遮挡播放画面；右侧空间不足时自动回落到左侧）。
  - 涉及文件：`public/app.js`、`public/styles.css`
  - 技术细节：
    - `renderEpisodeList` 的选集行增加 `data-title="完整集名"`；
    - 新增 `bindEpisodeTooltip()`（`mouseover`/`mouseout` 事件委托 + 面板 `scroll`、窗口 `resize` 时收起）；
    - 提示标签是挂在 `document.body` 的独立元素（portal 方式），避免被选集面板 `overflow-y: auto` 裁剪；
    - 定位逻辑 `showEpisodeTooltip()`：优先行右侧（`rect.right + 10`），放不下再放左侧，垂直居中并钳制在视口内；
    - 样式 `.bilinest-ep-tooltip`：深色圆角气泡、`pointer-events: none`、`max-width: min(340px, 60vw)`。

- **DASH 自适应码率播放**：B站视频从 durl（单一 mp4 地址）切换为 DASH 协议，dash.js 根据网络带宽自动切换码率。
  - 涉及文件：`server.mjs`、`public/player.js`、`public/index.html`、`public/vendor/dash.all.min.js`、`public/vendor/artplayer-plugin-dash-control.min.js`
  - 技术细节：
    - 服务端新增 `/api/dash.mpd`：用 WBI 签名请求 `/x/player/wbi/playurl`（fnval=4048），将 B 站非标准 DASH JSON 转换为标准 MPD；
    - MPD 仅保留 H.264（avc1）+ AAC（mp4a）编码，过滤 HEVC/AV1 避免 Chrome 硬解失败；
    - 视频/音频流通过 `/api/video` 本地代理转发（带正确 Referer），MPD BaseURL 为相对路径；
    - dash.js 通过 `httpHeaders` 透传用户 Cookie，确保高清晰度不被风控拦截；
    - 前端 `customType.dash`：ArtPlayer v5 以 `fn(videoEl, url, art)` 形式调用，切集时先 `art.dash.reset()` 释放旧实例。

- **清晰度下拉选择**：新增 `artplayer-plugin-dash-control` 插件，由 dash.js 码率列表自动生成清晰度下拉菜单。
  - 涉及文件：`public/player.js`、`public/vendor/artplayer-plugin-dash-control.min.js`

- **字幕改用 ArtPlayer 原生 VTT 组件**：移除自绘字幕层，B站 CC 字幕转为 WebVTT 格式（Blob URL），通过 `art.subtitle.switch()` 加载。
  - 涉及文件：`public/player.js`
  - 技术细节：
    - 新增 `subtitleBodyToVtt()` 将 `{from,to,content}[]` 转为 WebVTT 文本；
    - 用 `URL.createObjectURL` 生成 Blob URL，切集/重置时自动 `revokeObjectURL` 防止内存泄漏；
    - 字幕位置/字号设置仍可通过控制条微调，持久化到 localStorage。

### Fixed（修复）

- **CSP `worker-src` 缺失**：添加 `worker-src 'self' blob:`，解决 dash.js WebWorker 加载被 Content Security Policy 拦截。
  - 涉及文件：`public/index.html`

- **全屏弹幕加速**：全屏切换时弹幕速度异常变快。
  - 涉及文件：`public/player.js`
  - 技术细节：覆盖 `dp.resize`，在插件重置 transition 前按宽度比例修正 `$restTime`；新增 `lastDanmakuWidth` 状态追踪容器宽度。

- **弹幕重复/残留**：切集后弹幕堆积不消失。
  - 涉及文件：`public/player.js`
  - 技术细节：`p.load()` 改为无参调用（重置队列后重新加载），避免追加模式导致弹幕堆积。

- **选集面板执行顺序**：进入播放页时当前集未自动滚动到可见位置。
  - 涉及文件：`public/app.js`
  - 技术细节：面板先显示 → `sizeEpisodePanel()` → 再渲染列表 → 最后滚动到当前集，修复 `offsetTop` 为 0 的布局问题；`scrollEpisodeToActive` 增加 `Math.abs` 阈值判断（差值超过一行才滚动），避免切集时无意义跳转。

- **弹幕 protobuf 解码崩溃**：异常弹幕包导致整包解析失败。
  - 涉及文件：`server.mjs`
  - 技术细节：`decodeDmSeg` 增加单条 try/catch + wireType 校验，`vInt`/`vStr` 按 wireType 取值，异常包跳过不拖垮整包。

- **选集面板溢出**：剧集数较多时选集面板撑满屏幕。
  - 涉及文件：`public/styles.css`
  - 技术细节：`.episode-panel` 添加 `max-height: calc(100vh - var(--topbar-h) - 40px)`。

- **dash.js 实例泄漏**：切换视频时旧 dash.js 实例未释放。
  - 涉及文件：`public/player.js`
  - 技术细节：`reset()` / `loadBili()` 中先 `art.dash.reset()` 释放旧实例，再创建新实例。

- **视频加载错误诊断不足**：本地文件播放失败时提示信息过于笼统。
  - 涉及文件：`public/player.js`
  - 技术细节：`handleLoadError` 新增 `console.error` 输出 error.code + currentSrc；本地文件失败提示包含具体错误码文本（请求中止/网络异常/解码失败/地址不支持）。

### Changed（变更）

- **移除旧版 durl 播放重试/换源逻辑**：清晰度切换与重试由 dash.js 内部完成。
  - 涉及文件：`public/player.js`
  - 技术细节：移除 `MAX_URL_ATTEMPTS`、`MAX_FRESH_TRIES`、`MAX_QUALITY_TRIES`、`switchTo`、`reFetchPlayurl`、`fetchPlayurl`、`buildQualities`、`selectQuality`、`toProxiedUrl`。

- **移除自绘控件**：弹幕开关、字幕位置滑块、字号下拉改由插件/原生 API 处理。
  - 涉及文件：`public/player.js`
  - 技术细节：移除自绘弹幕开关控件（保留插件内置 `display` 控制）、自绘字幕位置滑块/字号下拉（改用原生 subtitle API）；`showBiliControls` 控件列表精简为 `['danmaku', 'subtitle']`。

- `launcher.vbs` 注释翻译为英文。

- 弹幕插件配置新增 `emitter: false`，隐藏"发弹幕"输入框（只读观看模式）。

---

## [v1.0.1] - 2026-08-28

### Added（新增）

- **弹幕完整度大幅提升**：弃用只返回“实时弹幕池”的旧 XML 接口，改用官方网页端分段弹幕接口（每 6 分钟一包、每包最多 6000 条），逐段拉取合并、按时间排序渲染；接口失败自动回退旧 XML。
  - 涉及文件：`server.mjs`、`public/player.js`、`public/api.js`、`README.md`
  - 技术细节：
    - 服务端新增 `/api/danmaku/segments`：优先请求 WBI 签名版 `x/v2/dm/wbi/web/seg.so`，失败回退无签名 `x/v2/dm/web/seg.so`；
    - 零依赖手写 protobuf 解码（`readVarint` / `readBytes` / `decodeDanmakuElem` / `decodeDmSeg`），兼容新旧两种字段结构（content 在 6 号或 7 号字段）；
    - `DmSegMobileReply` 的 1 号（elems）与 2 号（dmdm 补充弹幕）字段都解析合并；
    - B 站 CDN 对“无更多弹幕”返回 HTTP 304，视为空包正常结束（之前会误判为失败）；
    - 前端 `fetchDanmakuSegments`：从第 1 段顺序拉取，`elems.length < 6000` 即停止，上限 250 段（≈25 小时视频），段间 120ms 限速；第一段失败才回退 XML，后续段失败保留已拉数据；
    - 全局频率限制 `RATE_MAX` 由 40 提到 120 次/10s（本地保护，B 站仍有自身风控）。

- **服务端弹幕分段代理**：见上条（`/api/danmaku/segments` 即该项承载）。

- **端口占用自动顺延**：默认端口 4173 被其它程序占用时自动顺延到 4174、4175 …（最多 50 个），启动脚本自动打开实际端口。
  - 涉及文件：`server.mjs`、`launcher.vbs`、`start.sh`、`public/api.js`、`.gitignore`、`README.md`
  - 技术细节：
    - `server.mjs` 启动改为 `startServer(port)` 递归重试（`EADDRINUSE`/`EACCES` 时顺延，`PORT_TRIES = 50`）；
    - 成功监听后把实际端口写入 `bilinest.port`（纯数字文本），`/api/shutdown` 时删除；日志打印 `server started at http://…（4173 被占用，已自动顺延）`；
    - 监听成功回调用 `server.on('listening')` 只挂一次（`server.listen(port, cb)` 的 cb 在失败重试时会累积，导致日志重复）；
    - OAuth 回调地址 `oauth.redirectUri` 在端口确定后填充（未显式配置时跟随实际端口）；
    - `launcher.vbs` / `start.sh`：启动前删除旧端口文件，轮询等待后读取实际端口；健康检查校验响应体含 `bilinest`（防止 4173 被别的程序占用时误开别人的页面）；
    - 前端 `api.js init()` 从默认端口扫描 4173–4183 顺延区间，并校验 `info.app === 'bilinest'`；
    - 环境变量由 `BILIPURE_*` 改为 `BILINEST_*`（见改名条目）。

### Fixed（修复）

- 播放时弹幕不显示（需手动关闭再打开弹幕才出现）。
  - 涉及文件：`public/player.js`
  - 技术细节：ArtPlayer 的 `playing` getter 依赖 `currentTime > 0 && readyState > 2`，视频刚开始播放时误判为 false，导致弹幕动画循环从未启动；改用播放器自维护的 `state.playing`（由 `video:play/pause/ended` 事件置位），`renderLoop` / `startRender` 不再依赖 `art.playing`。

- 暂停后弹幕消失：改为暂停时冻结保留画面，便于暂停阅读；播放后继续滚动。
  - 涉及文件：`public/player.js`
  - 技术细节：`video:pause` 由 `stopRender()` 改为 `freezeRender()`（只取消 RAF、不清画布）；新增 `drawFrame()` 供动画循环与暂停静态帧共用；暂停中拖动进度（`video:seeked`）会重画当前时间点的静态弹幕。

- 暂停中拖动进度条弹幕错位：不再残留旧画面（见上条 `drawFrame` 复用）。

- B 站 CDN 对“该分段没有更多弹幕”返回 304 被误判为失败：现视为正常结束（见弹幕分段条目）。

- 逆向弹幕（mode 6）方向错误：改为从左向右正确渲染。
  - 涉及文件：`public/player.js`
  - 技术细节：`drawFrame` 增加 `d.mode === 6` 分支，`x = (t - d.start) * d.speed - d.width`。

- 端口顺延时启动日志重复输出（见端口顺延条目，`server.on('listening')` 只挂一次）。

- `launcher.vbs` 报 `800A0408 无效字符`。
  - 涉及文件：`launcher.vbs`
  - 技术细节：上一版误加 UTF-8 BOM，Windows 脚本宿主不认；已移除 BOM（该文件中文仅在注释中）。**注意：`create-shortcut.ps1` / `make-icon.ps1` 必须保留 UTF-8 BOM**（PowerShell 5.1 按 ANSI 读无 BOM 的 .ps1 会导致中文乱码）。

- 桌面快捷方式中文描述乱码（见上条 BOM 说明；快捷方式需重新运行 `create-shortcut.ps1` 生成）。

### Changed（变更）

- **项目改名 BiliPure → BiliNest**：程序内名称全面替换。
  - 涉及文件：`package.json`、`server.mjs`、`public/*.js`、`public/index.html`、`public/oauth_done.html`、`public/styles.css`、`launcher.vbs`、`start.sh`、`create-shortcut.ps1`、`make-icon.ps1`、`.env.example`、`README.md`、`LICENSE`、`.gitignore`
  - 技术细节（改名时同步替换的标识符）：
    - 包名 `bilipure` → `bilinest`；页面标题 / 品牌名 / 文案 → BiliNest；
    - 全局对象 `BiliPureStore/API/Local/Player` → `BiliNest*`；
    - 自定义事件 `bilipure-pause/ended/timeupdate/resumed` → `bilinest-*`；OAuth 弹窗 `bilinest-oauth`；
    - 请求头 `X-Bilipure-Sid` → `X-Bilinest-Sid`（服务端 CORS 白名单与读取、前端发送同步）；
    - 健康检查 `app: 'bilipure'` → `'bilinest'`（`api.js` 与 `launcher.vbs` 校验同步）；
    - 日志/端口文件 `bilipure.log` / `bilipure.port` → `bilinest.log` / `bilinest.port`；
    - 环境变量 `BILIPURE_*` → `BILINEST_*`；
    - CSS 类 `.bilipure-ctl` → `.bilinest-ctl`；ArtPlayer 容器 `id: 'bilipure'` → `'bilinest'`。
  - 保留未改：README 中的 GitHub 仓库链接与目录树根名（仓库层面由发布者处理）、本地文件夹名。

- **数据无损迁移**：改名不丢用户数据。
  - 涉及文件：`public/storage.js`、`public/localfiles.js`
  - 技术细节：
    - localStorage 新键 `bilinest.state.v1`，`load()` 检测旧键 `bilipure.state.v1` 后一次性迁移并删除旧键（登录态 / 收藏夹 / 观看记录 / 星级不丢）；
    - IndexedDB 新库 `bilinest-files`，`migrateLegacyDb()` 把旧库 `bilipure-files` 的本地视频文件句柄复制到新库后删除旧库；
    - `clearAll()` 同时清理新旧键。

- 桌面快捷方式由 `BiliPure.lnk` 更新为 `BiliNest.lnk`（重新运行 `create-shortcut.ps1`，旧快捷方式已删除）。

### Other（其他）

- `README.md`：弹幕实现说明、端口占用自动顺延说明、OAuth 回调注意事项、CHANGELOG 入口。
- `.gitignore`：加入 `bilinest.port`。
- 网络 / 代理排查（用户侧配置，非本项目代码变更）：诊断确认“开启代理后视频无法播放”的根因是 Clash 处于全局模式导致分流规则不生效；已为用户 Clash Verge Rev 当前订阅的规则文件（`rrR32aypmZRc.yaml`）添加 B 站系域名直连规则（bilibili.com / bilivideo.com / bilivideo.cn / hdslb.com / biliimg.com / biliapi.net / mountaintoys.cn → DIRECT），原文件已备份为 `.bak`。
